# DeepSeek 研判：W2 HTML 互动课件问题与 Skill 重构方案

- 日期：2026-08-13
- 作者：DeepSeek（AI 研判，非人类审批，不具备任何批准效力）
- 回应对象：[docs/reviews/W2_HTML_COURSEWARE_PROBLEM_STATEMENT_20260813.md](docs/reviews/W2_HTML_COURSEWARE_PROBLEM_STATEMENT_20260813.md)
- 范围：诊断、根因分层、优先级、最小充分门禁、Skill 重构方案。未修改任何代码或文件（本说明本身除外）。
- 调研基础：两份 Skill 全文（SKILL.md、case-templates、references、scripts）、问题说明引用的编辑器/Player 源码事实（经代码核实）、`validate:project` 与三个 build 侧校验器、`validate_case.py`。

## 1. 总体判断

问题说明的根因分层基本准确，但还漏了三层更底的东西：

1. **渲染平面合成模型缺陷**（比"z-index 没排好"更深）：`TeacherControllerNode` 是 Phaser canvas 内对象，全屏场景 Runtime 是 DOM overlay。Phaser canvas 是单一 z=2 平面，内部 depth 无论如何都越不过 DOM z=3。这是"全局控制层与场景内容层共享合成模型"的结构性缺陷，不是调 z 值能解决的。
2. **编辑性只有声明、没有证明**：Authoring Inventory 的 `visible` 由 Builder 自我声明，没有机制证明"逐对象可定位"成立。语文 86 个 Runtime 实体共享整幕大区域 bounds 也能标 `visible`。这是"声明制"与"证明制"的差距。
3. **单机判题能力在产品 API 里根本不存在**：交互系统触发器只有 `node.activated / animation.completed / presenter.command` 等，没有"答案匹配"类触发器；`src/` 无任何答案归一化/数学等价/开放回答评估器。上游合同假设"有成功标准 ⇒ 页面自动判定"，而产品从未提供这个能力，Builder 只能手写正则——这是问题说明 5.2.C/D 的深层共同根因。

另一个系统性结论：**`engineering candidate` 语义已漂移**。现有门禁证明的是"产物真实 + 结构有效 + 一条黄金路径可达"，与"教师可用的 HTML 行为候选"是两回事。评级体系把两者合并了。

## 2. 根因分配

| 层 | 判断 |
| --- | --- |
| 教学合同 | **有实质缺陷，需重开**。容量只用分钟求和（`validate_case.py` 的 ±1 分钟检查），没有必答单元/输入时间/重试/讨论的可检验预算；"学习证据"未区分采集方式（页面必采/口头/纸笔/讨论）；判定权威未冻结（成功标准 ≠ 自动判定权）；能力假设（全班分布等）未经确认 |
| 呈现脚本 | **部分缺陷**。教师控制、重试、揭示、恢复确实写了（问题说明 5.1 已确认），但缺三样可契约化条款：①每场景的**教师逃生路径**（错误/空白态下教师可执行动作，不依赖学生答对）；②每个证据动作的**采集方式标注**；③每个可判点的**判定权威（T1–T4）**。模板本身就没有这些字段，作者无从填写 |
| Builder | **主要执行侧责任**。载体选择把稳定内容整体让渡 Runtime（语文 86/86）；`includeDefaultController:false` + 手工控制器却不回改 `playback.controls`（工程自相矛盾）；把"有成功标准"误读为"Runtime 手写正则硬判"；脚本要圈画，实现用"错误/完成"按钮直接切状态 |
| Editor/Player | **产品缺陷，Skill 无法自愈**。`editorStore.ensureTeacherController()` 在节点已存在时只定位不修 `controls`（坏状态永续）；Player 跨平面层级缺陷（见第 1 节第 1 点）；Authoring Inventory 无逐对象定位证明机制 |
| 校验与评级 | **门禁方向错了**。`validate_evidence.py` 校验的是**结构真实性**（文件格式、哈希、帧角色、编辑往返字节变化），完全没有**行为真实性**（控制器可点、圈画真实、答案容错）；`engineering candidate` 在 Schema/导出绿色时即可写 |

## 3. 对八个问题的回答

**Q1（分层是否准确）**：准确，但遗漏第 1 节三点底层问题。

**Q2（控制器留 Phaser 还是独立 DOM 平面）**：推荐**独立全局 DOM 控制平面**（专用顶层，不与 scene overlay 竞争）。理由：控制器是元层，不属于任何场景画布；留在 Phaser 内跨平面 z 序问题不可解。分两步走：
- 近期（薄、稳定）：修 `ensureTeacherController` 已有节点时也强制 `controls='canvas'` + 校验器强制一致性 + 行为门禁（截图证明控制器可见可点）。不依赖合成模型重构。
- 中期（产品另案）：控制器迁出 Phaser 到独立 DOM 平面，z 高于一切场景层；全屏 scene Runtime overlay 不得覆盖。

**Q3（编辑性边界：硬比例还是类别门禁）**：**按内容类别门禁，不设硬比例**。硬比例会被凑数且惩罚真正需要 Runtime 的场景。类别下限见第 6.2 节。

**Q4（判题模型）**：四层权威，在合同里冻结：
- **T1 有限选项**（单选/多选/配对）→ 自动判定；
- **T2 规范化可判短答案**（数值、固定形式坐标、短词填空）→ 共享归一化后自动判定；
- **T3 数学等价表达**（f(x)=0 形式、根集合、表达式化简）→ 需要真等价层（AST/符号计算），**没有就降级 T4，禁止手写正则硬判**；
- **T4 开放回答/证据表达**（圈画、引用、解释、40 字结论）→ **一律不自动判定**：自检提示/参考对照 + 教师判定/互评，**导航不得以 T4 为唯一条件**。
"根本不应自动判定"的：一切语义开放表达。40 字结论连"长度够"都不能代替语义判定。

**Q5（发现"圈画变按钮"的语义失真）**：**清单比对 + 禁止直写状态**。脚本的每个 EVD 动作 → Builder 在计划里声明实现载体（哪个控件）→ E2E 证据通过真实控件产生状态变化；校验器比对脚本动作清单 ↔ 实现动作清单 ↔ 证据条目一一对应；规则上明确**禁止直接设置 Presentation State 冒充学生动作**。不需要语义理解，清单闭合即可抓住这类失真。

**Q6（轻量容量预算）**：给每类动作一个默认时间档（输入 20–30s、选择 10–15s、圈画 15–20s、阅读 200 字/分钟、40 字书写 60–90s），合同按"必答单元 × 首次作答 + 一次典型重试 + 教师讨论 + 阅读/观看"求和，超过时长即阻止进入实现。校验器只做求和与对比，不做精细计时。**关键改变：从"各阶段分几分钟"变为"必答单元可枚举"。**

**Q7（engineering candidate 最低行为证据）**：自动可证的全部下沉到候选门禁——控制器每幕可见可点、逃生路径、动作真实性、答案容错矩阵、T4 无硬锁、编辑闭环按载体取样、容量通过、工程一致性。留人类验收的：教学有效性、视觉审美、真实课堂节奏、编辑体验满意度、语义判定正确性（这些进 `accepted`）。

**Q8（产品先行 vs Builder/校验器）**：先修产品的四项（均为另案，Skill 无法自愈）：①`ensureTeacherController` 已存在时修 `controls`；②控制器合成模型（Q2 近期步骤）；③共享答案归一化层 + T2/T3 判定 API（或官方声明"单机无自动判题能力"）；④Runtime Authoring 逐对象定位能力。其余全部可经本次 Skill 重构解决。

## 4. P0 / P1

**P0（继续创作前必须解决）**：
1. 教师控制一致性 + 可达性门禁（工程自相矛盾 + 行为证据）；
2. 判定权威 T1–T4 冻结 + T4 不硬锁（上游合同 + Builder 规则 + 校验三层）；
3. 载体编辑性下限（稳定内容逐对象可编辑，拦截语文式 86/86 Runtime）；
4. 动作真实性清单比对（禁直写状态冒充）；
5. 响应容量预算（超预算阻止实现）。

**P1（可并行）**：控制器独立 DOM 平面重构；`visible` 证明制；共享归一化层设计；PDF/PPTX 轻量门禁固化。

## 5. 最小充分硬门禁（把 9 条收敛为 6 条）

1. **教师控制可达性**：每互动幕必须有一条 `controller-reachability` 证据（真实截图+交互证明控制器可见、最高层、上一幕/下一幕/重播/重置可执行）；
2. **教师逃生路径**：任意错误/空白/未完成状态，教师可揭示/跳过/继续，不得依赖学生先答对——每幕必须有 `escape-path` 证据；
3. **动作真实性**：每个 EVD 动作实现载体声明 + 真实控件 E2E 证据，禁止直写 Presentation State 冒充；
4. **判定矩阵**：每个 T1–T3 判定点覆盖标准答案、等价答案、全半角/空白变体、典型近错、拼接假阳性五类；T4 静态检查导航不唯一依赖其判定；
5. **HTML 编辑闭环按载体取样**：native 文本、公式、runtime text、素材各至少一个"改→存→关→重开→Player/HTML 更新"往返；`visible` 必须引用可定位证据；
6. **工程一致性**：`globalLayer` 含 teacher-controller ⇒ `playback.controls='canvas'`（纯静态，直接进 inventory 校验器）。

## 6. Skill 重构方案（文件级清单）

### 6.1 orchestrate-courseware

| 文件 | 改动 |
| --- | --- |
| `SKILL.md` | ①不变量新增 4 条：判定权威必须冻结为 T1–T4；证据动作必须标注采集方式；容量必须按必答单元预算；未经确认的产品能力不得成为脚本要求；②§3 合同段增加新章节指引；③§4 脚本段要求每场景写教师逃生路径与判定权威；④§2 决策门增加"产品能力确认"（有疑问问用户，Builder 不得猜造） |
| `assets/case-templates/01-courseware-contract.md` | 新增 5 个章节：**响应容量预算**（必答单元表：ID/动作类型/首次作答时间/是否允许重试/教师讨论时间，总和 ≤ 总时长，超则删减或降级采集方式）；**学习证据采集方式**（每 EVD 标注页面必采/口头抽查/纸笔对照/讨论非门禁）；**判定权威**（每可判点标 T1–T4，T4 写明教师/自评/互评且不作唯一导航条件）；**产品能力假设**（单机离线、教师控制、自动判定上限、多人数据——未确认一律"不要求"）；**HTML 编辑结果要求**（结果级：哪些类别必须逐对象可编辑、哪些允许整体 Runtime、编辑闭环一致性） |
| `assets/case-templates/02-presentation-script.md` | 场景小节：①反馈表加"判定权威（T1–T4）"列；②新增"#### 教师逃生路径"（错误/空白/未完成态教师的揭示/跳过/继续）；③学生动作逐条标注采集方式（EVD 关联）；④场景末尾加必答单元用时合计 |
| `scripts/validate_case.py` | ①`CONTRACT_HEADINGS`/`SCRIPT_HEADINGS` 增加新章节检查（复用 `completed_section`）；②容量预算求和校验：必答单元时间总和 vs 场景用时 vs 总时长，超预算 error 并阻止 `implementation-ready`；③T4 检查：T4 内容出现在导航条件中且无教师接管 → error；④能力假设检查：出现"全班/多人/分布"等关键词而无对应确认 → error；⑤每场景必须有逃生路径小节。注意：这会令 W2 旧课例失败，属预期 |
| `references/review-rubrics.md` | 合同 rubric 增加容量预算/采集方式/判定权威/能力假设/编辑结果五项；脚本 rubric 增加逃生路径与判定权威列；拒绝条款加"时间仅按分钟求和""T4 被自动硬判""证据动作无采集方式" |

### 6.2 build-project-v8-courseware

| 文件 | 改动 |
| --- | --- |
| `SKILL.md` | ①规则 4 改写：载体选择前必须先读合同的"HTML 编辑结果要求"与"判定权威"；②新增规则：一律使用默认控制器（不改 `includeDefaultController:false`）；若 globalLayer 含控制器必须同步 `playback.controls='canvas'`，实现后必须验证控制器可见性；③新增规则：判定只实现合同冻结的权威，T4 禁手写正则硬判，T2/T3 必须用共享归一化层（当前不存在 ⇒ 降级 T4 并回编排改合同），禁止 Runtime 手写答案正则；④新增规则：证据动作必须经真实控件 E2E 产生，禁直写 Presentation State；⑤规则 9 强化：稳定内容从画布可编辑降为 Runtime 属性入口 = 载体降级，必须回编排，不得仅披露差异后继续；⑥验证与交付段增加四条新证据类别 |
| `references/carrier-selection.md` | ①新增**内容类别 → 编辑性下限**表：稳定题面/正文/标签/按钮文字/反馈/教师提示/公式/素材/输入控件 ⇒ 原生节点或结构化逐对象绑定；复杂动态图象/算法/连续交互 ⇒ Runtime 可整体拥有但需静态后备 + 降级披露；②新增**全局控制层**节：控制器必须全局可见、最高层，全屏 Runtime overlay 不得覆盖，实现时必须验证；③新增**降级显式化**节：定义载体降级（编辑能力降级 ⇒ 回编排），与"正常差异披露"分界 |
| `references/authoring-inventory.md` | ①`visible` 收紧为证明制：必须能逐对象定位并引用定位证据；Runtime 大区域/整幕 bounds 只允许 `property-only`；②新增 `judgeAuthority: T1–T4` 字段（来源合同 CNT-*）；③T4 实体标注"不作导航唯一条件" |
| `references/export-and-evidence.md` | ①证据类别新增：`controller-reachability`（每互动幕）、`escape-path`（每幕）、`answer-tolerance`（每 T1–T3 判定点五类矩阵）、`action-authenticity`（每 EVD 动作）；②编辑闭环按载体类别取样；③E2E 执行器固定为 agent-browser 类真实交互，截图不得跨证据槽复用 |
| `scripts/validate_evidence.py` | ①schema 扩展新证据类别并校验完整性（缺类即 fail）；②控制器/逃生/动作证据必须是截图或录屏且哈希不与其他槽重复；③`acceptance_scope` 纳入新字段（旧验收自动失效） |
| `scripts/validate_authoring_inventory.py` | ①新增工程一致性静态门禁：globalLayer 含 `teacher-controller` ⇒ `playback.controls` 必须 `'canvas'`；②新增载体下限门禁：场景 `runtime-owned` 且含稳定文本类实体而无逐对象绑定 ⇒ error；③`judgeAuthority` 字段校验（T1–T4 合法、T4 不绑定为导航唯一条件）；④`visible` 必须引用证据条目 |
| `scripts/validate_v8_case.py` | `REQUIRED_PLAN_HEADINGS` 增加"## 判定与逃生设计""## 动作真实性清单"；计划必须绑定合同判定权威与容量预算的哈希 |

### 6.3 产品侧配套（另案，不在本次 Skill 重构内）

1. `editorStore.ensureTeacherController()`：已有节点时也强制 `controls='canvas'`；
2. Player 合成模型：控制器独立 DOM 平面（或至少修复跨平面覆盖并加验证）；
3. 共享答案归一化层 + T2/T3 判定 API（或官方声明无此能力，Builder 按声明降级）；
4. Runtime Authoring 逐对象定位能力（支撑 `visible` 证明制）。

## 7. 影响面与迁移

- **W2 两课例**：新校验器下会失败（控制器一致性、载体下限、T4 硬锁、容量预算均不满足）——符合预期，按问题说明第 10 节保留为失败样本，重开时走 orchestrate 恢复流程补齐五处合同缺口。
- **模板版本**：合同/脚本模板新增章节后，`init_case.py` 生成的新课例自然带新结构；旧课例恢复时校验器会以"缺章节"报错并指向最早返工阶段（现有机制已支持）。
- **校验器兼容**：`validate_case.py` 的 heading 检查是叠加式，只影响未补齐旧课例，不影响机制本身。

## 8. 不建议采用的方案

- **扩大正则列表 / 关键词库**：问题说明 4.4 已证伪，假阴假阳都会更多；
- **Runtime/原生硬比例**（如"原生 ≥ 40%"）：可凑数、惩罚合理 Runtime 场景、与内容类别无关；
- **纯文案补丁**（"再补几条 Skill 文案"）：不改变校验器和模板的结构，W2 会以新形态复发；
- **围绕 PDF/PPTX 对象级还原做大规模重构**：问题说明已明确其为兼容形态，不反向主导 HTML；
- **在产品修复完成前给 Builder 加"自愈"逻辑**：Builder 修不了 `ensureTeacherController` 和跨平面层级，让它绕过只会产生第二套影子机制。

## 9. 仍需补充的证据或问题

- 语文"圈画"所需的真实采集控件在产品里是否存在（拖选区域/高亮）？若无，语文 T4 证据采集需要产品提供，或降级为纸笔+教师对照；
- 数学 T3 等价判定：平台现有 `FormulaNode` 的 AST 能否支撑根集合/表达式等价，还是需要引入符号计算？
- agent-browser 技能能否作为 E2E 行为证据的标准执行器（判断是可以，但需在 Skill 里固化为唯一入口）。

## 10. 待人类拍板的点

1. **Q2 走向**：控制器独立 DOM 平面为中期目标；近期先用"一致性修复 + 可达性门禁"过渡——两步走是否成立？
2. **T2/T3 判定能力**：产品侧暂无归一化层。默认方案"没有就降级 T4 并回编排"，即当前阶段所有非有限选项都走教师接管。先做共享归一化层（产品另案）还是先按降级路线跑通流程？
3. **动作真实性证据执行器**：是否同意把 agent-browser 固化为 E2E 行为证据的唯一入口？
4. **范围确认**：产品侧四项是否单独立项（编辑器仓库另行处理），本次只动 `.agents/skills/` 下两个 Skill？
