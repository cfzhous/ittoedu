HANDOFF
- task: R0-C
- planning pack path: `C:\Users\74755\Documents\HTML课件编辑器\docs\tasks\v8-to-v9-rebuild`
- product worktree / branch / baseline SHA: `C:\Users\74755\Documents\HTML课件编辑器-v8-to-v9-rebuild` / `codex/v8-to-v9-rebuild` / `f27275658c6dfaa12f2ce35cd9368dcdebe99451`
- outcome: 只读核对产品 worktree 与供体 Git 对象；交付完整供体矩阵。`f272756` 确认为成熟 V8 路径图（`main.tsx` → `App`，无 ProductApp）。`14890bb` 证明在 `3e41ec0` 之后且默认 ProductApp 走 Course Studio V9，只登记为行为地图。`79c821f..f272756` 核心壳零 diff，R0-G 默认不必二分。V9 协议/archive/producer/Slide/Flow/Spatial 供体按文件列出；HEAD/`bffbf95` 只作反例与函数级摘取。未改任何产品源码，未开始 R0-B/D/G 或 R1+。
- owned files changed: `docs/tasks/v8-to-v9-rebuild/artifacts/R0_C_DONOR_MATRIX.md`；本 HANDOFF。无产品源码改动。
- donor files/functions consulted: 见矩阵全文。重点：`f272756:src/renderer/main.tsx`、`App.tsx`、`store/editorStore.ts`、`ui/Workspace.tsx`、`ui/RightSidebar.tsx`、`ui/ElementsTab.tsx`、`ui/MediaTab.tsx`、`project/*`、`player/index.ts`、`export/*`、`shared/runtimeTypes.ts`、`shared/componentTypes.ts`；`14890bb:ProductApp.tsx` / `v8-behavior-map.json`；`3e41ec0` 起 `courseProject*` / `publishedCourse*` / `authoringAddress` / `courseProjectArchive` / `buildPublishedCourseV2Payload`；`e2e34aa` 树快照中的 Flow/Spatial/Slide 模块；`6361641` App 热点 diff（禁覆盖）；`4755034`/`bffbf95` controlled UI 与 lifecycle/location/global 命令（反例 + 纯函数）。
- focused validation command: 只读 Git/`rg`；计划目录 `git diff --check -- docs/tasks/v8-to-v9-rebuild`。未跑 Vitest。
- validation result: 路径与祖先关系已用 `git ls-tree` / `git show` / `git merge-base --is-ancestor` 核实。diff check 见本任务末次命令输出。
- validation entry / fixture / backend: n/a（纯文档审计；产品 worktree 仍为 V8 `App` / V8 `ProjectDocument`，本任务未启动）
- validation proves / does not prove: 证明 `f272756` 入口与模块真实路径、`14890bb` 非 V8 基线、`79c821f..f272756` 编辑器 diff 范围、V9 供体文件/函数与禁止项。不证明 UI 能力可达、格式隔离、或任何协议 round-trip。
- narrow UI smoke, if authorized: 未授权；未启动。
- INTEGRATION_REQUESTS: 无
- DECISION_REQUESTS: 无。R0-G 可参考矩阵 §3：除非 R0-B 在控制器/新建工程/`updatePlayback` 上复现严重回归，否则不必向 `79c821f` 二分。
- remaining risks / untested full checks: 未跑 typecheck / 全量或定向 test / build / E2E / 视觉回归（R8）。Flow 供体 Schema 仍为 `text: string`，R1-A 需补 runs，不能把 HEAD Schema 当成已完成强文本合同。`e2e34aa` 是文档提交上的集成树快照，不是代码变更包。`courseProjectProtocol.test.ts` 在 HEAD 已耦合 location/lifecycle，不可当 R1 定向测试整文件迁入。
- rollback point: 删除本任务两个 markdown 文件即可；产品 worktree 未被修改。
- execution state: lane_candidate
- integration state: n/a
- quality state: unverified
- canonical product worktree: `C:\Users\74755\Documents\HTML课件编辑器-v8-to-v9-rebuild`
- exact baseline SHA: `f27275658c6dfaa12f2ce35cd9368dcdebe99451`
- launch command/result: n/a（交 R0-A 已记录：`npm run dev` 成功）
- V8 capability inventory summary: n/a，交 R0-B
- baseline screenshots/video locations: n/a
- format/recovery isolation result: n/a，交 R0-D
- teacher decision: 待 R0-G
