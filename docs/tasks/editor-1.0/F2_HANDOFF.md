# F2 HANDOFF
- 范围：`FlowBlockProperties` 块类型下拉选择（段落 / 引用 / 一级到六级标题）及从 `runs` 读取文字颜色
- 合同是否变化：否
- 分支 / SHA：`cursor/f2-flow-block-type-44bf`
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/flowProductIntegration.test.tsx` (7 passed)
  - `git diff --check` (clean)
- 未验证（交给 T6）：全量 typecheck、全量 tests、e2e、desktop build
- 停下来的原因（若有）：无
- 下游：父代理合入 `cursor/flow-authoring-f-44bf`
