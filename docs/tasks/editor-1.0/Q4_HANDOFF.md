# Q4 HANDOFF
- 范围：选中 Flow `media` 块后可改 alt/caption/layout，用库内同 kind 素材替换 `assetId`，可删除该块；稿纸选中态增加 `data-flow-media-selected`。未做环绕/浮动，未接本地文件导入（需改 `editorStore` / `App.tsx`，按卡停手）。
- 合同是否变化：否
- 分支 / SHA：cursor/q4-flow-image-edit-489b
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/flowWorkspaceMedia.test.tsx`
  - `npx vitest run tests/unit/flowMediaBlockEdit.test.ts`
  - `git diff --check`
- 未验证（交给 T6）：PropertiesTab 挂到真实 editorStore 的点击路径、全量 e2e、本地文件替换接线
- 停下来的原因（若有）：本地文件选择必须改 `editorStore.ts` / `App.tsx`，按卡先交库内素材下拉替换。工人 `bc-700c2a49` 空转未改文件，由父代理在本分支补完。
- 下游：父代理合入；T6 可补 Properties 面板 DOM 断言与 e2e
