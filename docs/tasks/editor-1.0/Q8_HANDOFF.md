# Q8 HANDOFF
- 范围：更新过时的 e2e 图层树断言与注释（`tests/e2e/editor.spec.ts`、`tests/e2e/componentCatalogMatrix.spec.ts`）。默认真场景/恢复场景下教师控制器不再列入图层树（count 从 1 变为 0，相应总节点数减 1）；点击全局图层入口的用例保持 controller count = 1。
- 合同是否变化：否
- 分支 / SHA：`cursor/q8-e2e-controller-list-489b`
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `git diff --check`（clean）
- 未验证（交给 T6）：
  - 桌面 T6 应在桌面环境下重新运行改过的两个 spec：`tests/e2e/editor.spec.ts` 与 `tests/e2e/componentCatalogMatrix.spec.ts`。
- 停下来的原因（若有）：无
- 下游：父代理合入。
