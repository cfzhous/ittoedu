# Q1 HANDOFF
- 范围：编辑态教师控制器 inert（Slide 与 Spatial 世界编辑时不抢手势、不切换全局 scope）、NodesTab 图层树在非全局 scope 时过滤掉控制器且移除空分组、更新及增加相关单测。
- 合同是否变化：否
- 分支 / SHA：cursor/q1-controller-inert-489b
- 允许列表外改动（必须空，除非重命名机械 import）：无（严格遵守允许文件列表）
- 最小验证命令与结果：
  - `npx vitest run tests/unit/v9GlobalLayerUiAdapter.test.tsx` (9 passed, 1 test file)
  - `npx vitest run tests/unit/teacherControllerRuntimeSession.test.ts` (13 passed, 1 test file)
  - `git diff --check` (clean)
- 未验证（交给 T6）：全量 `npm test`、`npm run test:e2e`、`npm run build:desktop`、`npm run verify`
- 停下来的原因（若有）：无
- 下游：父代理合入（建议最先合入 Q1，以防止误入全局 scope）。
