# Q6 HANDOFF
- 范围：`editorStore.addVideoNode` 的 spatial 早退分支改为传入真实 `spatial` session 与 `input.asset`，移除临时 `withAsset` 假 session 构造，使 `addSpatialWorldVideoLayer` 能够原子写入 `draft.assets` 并插入世界视频原生图层；新建 `tests/unit/spatialAddVideoNode.test.ts` 进行单元测试。
- 合同是否变化：否
- 分支 / SHA：`cursor/q6-add-video-node-489b`
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/spatialAddVideoNode.test.ts`（2 passed）
  - `git diff --check`（clean）
- 未验证（交给 T6）：Playwright e2e、真机多格式视频解码播放、整课 Mixed 试运行
- 停下来的原因（若有）：无，完整达成任务卡所有要求
- 下游：父代理合入 `cursor/editor-q-stability-489b`
