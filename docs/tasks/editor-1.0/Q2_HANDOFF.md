# Q2 HANDOFF
- 范围：支持 global editingScope 下全局 Native 图层（除教师控制器外）在 Slide 画布中的选择、拖动、缩放、旋转及 Native 内容属性写入，并严格禁止教师控制器走 transformNativeLayers 及 updateSlideNativeLayerContent。
- 合同是否变化：否
- 分支 / SHA：cursor/q2-global-native-transform-489b / HEAD
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/v9SlideViewportAdapter.test.ts` (9/9 passed)
  - `npx vitest run tests/unit/v9SlideContentCommands.test.ts` (10/10 passed)
  - `git diff --check` (clean)
- 未验证（交给 T6）：
  - Workspace DOM overlay 挂载与视觉渲染
  - 全量整课发布与 Player 试运行
- 停下来的原因（若有）：无
- 下游：
  - 父代理合入与 T6 验收
