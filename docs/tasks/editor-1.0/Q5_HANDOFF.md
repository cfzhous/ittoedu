# Q5 HANDOFF
- 范围：`SpatialSurfaceHost` 从 Published `assets[].url` 解析视频；世界视频改为 SVG 旁的 HTML `<video controls>` 并跟相机 CSS 变换；当前位置试运行显式传入 `resolveAsset`；`addSpatialWorldVideoLayer` 可在缺 assets 条目时用可选 `asset` 元数据写入后再插入。未改 `editorStore.addVideoNode`。
- 合同是否变化：否
- 分支 / SHA：cursor/q5-spatial-video-489b
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/spatialSurfaceHost.test.ts`
  - `npx vitest run tests/unit/spatialLocationTryRun.test.ts`
  - `git diff --check`
- 未验证（交给 T6）：Electron 真机解码、整课 Mixed 预览、`addVideoNode` 接线
- 停下来的原因（若有）：工具栏插入视频仍走 `editorStore.addVideoNode`，按卡不改 store。工人 `bc-0efd50d0` 空转未改文件，由父代理在本分支补完。
- 下游：父代理若要工具栏插入，再补 `addVideoNode` 把 `asset` 元数据传给 command
