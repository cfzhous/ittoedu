# HTML 课件编辑器内部正式版 1.0 与多表面后续开发计划

> 当前主干：Editor 1.0.0 收敛分支 / Project V8 / Runtime API 2 / Component API 4
>
> 已归档基线：内部原型 Editor 1.7.0 / Project V7（`internal-prototype-1.7.0`）
>
> 目标基线：内部正式版 Editor 1.0.0 / Project V8（仅幻灯片表面）
>
> 计划状态：里程碑 0、里程碑 1 已完成；里程碑 2 的首批九个 experimental 组件已通过最终隐藏 V8 矩阵 2/2，里程碑 3 的既有软件实现与 S6 同工作树验收结果为 `engineering candidate`。2026-08-12 追加的 AI-native 编辑器基建 P0–P4 已完成实现与自动化验收，P5 完成 RFC/测试原型并停在生产门禁；Skill/工作流、物理课例仓库、新课件表面与最终产品验收尚未开始
>
> 决策日期：2026-08-07
>
> 目标：先把现有幻灯片/PPTX 兼容模式收敛成可内部投产的正式版 1.0，再逐步验证流式长文、空间画布与混合工程。

本文是后续产品与工程工作的路线图，不表示下述模式已经可用。当前已实现能力仍以 [README](README.md)、[用户指南](docs/USER_GUIDE.md) 和 [AI 互动课件创作规范](docs/AI_COURSEWARE_AUTHORING.md) 为准。在正式版 1.0 或新模式真正完成 Schema、编辑、播放、保存、导出和验收前，不得在用户文档中把计划能力描述为已交付功能。

正式版 1.0 仅供内部使用，当前不以对外销售或分发为目标。许可证调整、代码签名、公开安装器、自动更新和外部分发渠道不属于本轮发布门禁；可继续使用受控源码入口或内部未签名构建完成验收。

## 1. 已确认的产品决策

1. **互动 HTML 是所有模式的第一交付物。** 单 HTML 与网页包必须保留各模式的原生阅读、互动、导航、状态和媒体语义。
2. **PPTX、DOCX 和 PDF 都是兼容导出，而不是产品真相。** 它们按目标格式能力保留原生对象或生成明确的静态后备，不反向限制 HTML 成品必须长得像对应办公软件。
3. **内部正式版 1.0 只生产化幻灯片模式。** 默认新建仍进入固定 1280×720 幻灯片，它继续承担课堂演示、投影讲授、逐幕互动和对象级 PPTX 兼容；Flow、Spatial 2D 和混合工程不进入 1.0。
4. **正式版 1.0 执行一次受控断代。** 因尚无正式投产用户，不继续承担 Project V1–V7、Runtime API 1 和 Component API 1–3 的运行兼容；先归档当前 1.7.0 基线，再从主干移除旧解析、迁移、适配、样例和专属测试。
5. **应用版本与工程格式分开。** 应用可在未公开分发的前提下重置为正式版 `1.0.0`；新工程使用 Project V8，不能把工程版本重置为 V1。正式版 1.0 只接受 Project V8、Runtime API 2 和 Component API 4。
6. **编辑器核心、组件库和课例库分离。** 核心仓库的目标职责只包含产品代码、规范和当前协议最小测试夹具；可复用组件进入独立组件库，课例档案与成品进入独立课例库。S6 只冻结课例库职责和迁移清单，不以创建空仓库或移动历史课例冒充软件能力；用户批准 W1 后才物理建库并原子迁移现有旗舰/失败课例。
7. **外部库用于发现，工程内嵌用于交付。** 编辑器可以读取内置、团队、个人和课例仓库内的组件目录，但使用组件时必须校验、复制、锁定版本与哈希并嵌入 `.h5lesson`；成品不得运行时依赖外部仓库路径。
8. **两个课件 Skill 中文化。** 保留英文稳定 ID、命令、Schema 字段和事件名；触发描述采用精炼中英双语，正文、参考规范、默认提示和错误说明改为中文，并用冻结测试集验证无回归。
9. **教师全局控制器在成品 HTML 中默认整面可拖动。** 不划分按钮区和拖拽区，而由统一位移阈值区分点击与拖动；作者位置是初始位置，授课拖动是会话临时状态，切幕和重播保持，重开课程恢复，收起后可贴边停放，静态导出仍默认省略。
10. **正式版 1.0 支持通用翻页笔。** 不识别具体品牌，而把标准键盘/HID 输出转换为 `next`、`previous` 等演示命令；默认支持 `PageDown/PageUp`，区分简单切幕与作者规则两种推进语义，任何切幕都不得绕过导航守卫。
11. **正式版 1.0 补齐语义化公式与输出预检。** 至少正确支持纵向分式、上下标和根式，禁止用斜线文本冒充正式分式；导出前检查文字溢出、缺失字体、组件捕获失败、空白页和静态差异。
12. **工作流有效性必须由全新冷启动课例证明。** 至少两个不同学科、不同互动机制的课例达到人工 `accepted`；修补旧失败课例不能作为正式版工作流成功证据。
13. **长文本第一阶段采用 Word/Notion 式流式块编辑。** 使用固定或最大阅读宽度、无限纵向增长、结构化内容块和章节折叠；不在第一阶段实现 OneNote 式任意位置文本容器。
14. **空间画布先做无限二维。** 通用三维场景编辑器不进入首轮范围；近期 3D 继续通过场景运行时或 V4 组件承载。
15. **混合模式是工程容纳多种表面的能力，不是第四种表面类型。** 用户可在同一课程中组合幻灯片、长文和空间画布，并由统一导航、主题、素材和课程状态连接。
16. **混合工程仍可导出互动 HTML。** 第一版混合工程的共同静态导出只保证 PDF；PPTX 和 DOCX 只对兼容表面提供单独或选择性导出，不对整课承诺无损转换。
17. **多表面不阻塞正式版 1.0。** 先完成幻灯片模式的断代、质量闭环和内部投产，再按价值与风险逐个验证新表面。
18. **作者评审辅助保持只读和最小。** Project V8 只增加机器可读的字体/色板 `designTokens` 与图片归一化 `safeAreas`；Project Health 的信息释放和视觉密度只提供可达性近似与启发式概览，不自动改写工程，也不建立第二套教学或视觉规则系统。
19. **当前捕获只承诺一个确定帧。** `capture.waitUntil()` 与 `prepareCapture()` 负责资源就绪和最终帧；动态过程多帧捕获保留为真实课例驱动的研究项，在时间、帧数、状态副作用和输出语义冻结前不引入第二协议。

## 2. 第一原则目标

### 2.1 用户结果

用户面对不同教学内容时，应能选择与内容天然匹配的创作表面：

- 需要投影讲授、节奏控制和 PPTX 交付时，使用幻灯片；
- 需要连续阅读、讲义、教程、研究笔记或知识库时，使用流式长文；
- 需要探索空间关系、概念地图、时间线、复杂系统或镜头漫游时，使用空间画布；
- 一门课程同时需要多种表达方式时，在同一工程中组合不同表面。

无论选择何种表面，用户都应获得：

- 可保存、重开和继续编辑的作者态工程；
- 可离线运行的互动 HTML；
- 与表面匹配的编辑、预览、缩略图和质量检查；
- 对兼容导出中保留项、静态化项和缺失项的明确说明；
- 管线状态与成品结果状态分离的验收报告。

### 2.2 不可妥协的成功标准

1. HTML 运行语义不因办公格式兼容而降级。
2. 人工创作、最终会被人看见的文字仍有结构化编辑入口。
3. 表面类型必须拥有清晰、互斥的数据模型，不能依赖大量可选字段拼成非法组合。
4. 正式版 1.0 只接受 Project V8、Runtime API 2 和 Component API 4；旧格式必须得到清晰、可诊断的“不受支持”结果，不能部分载入或静默迁移。
5. 每种表面都必须定义编辑、播放、缩略图、捕获、静态导出、重播、销毁和失败后备语义。
6. 混合工程必须保持统一导航、主题、媒体、课程状态和离线交付。
7. 不把“自由度更高”误当作“质量更高”；每种表面都必须有针对性的创作工作流和结果门禁。
8. 组件和课例源码可以位于外部仓库，但 `.h5lesson`、单 HTML 和网页包必须自包含、离线、可复现。
9. 正式版 1.0 必须用两个全新冷启动课例同时证明创作工作流、编辑器承载和导出质量。
10. 标准翻页笔在整课预览、单 HTML 和网页包中必须即插即用；互动课件的教师检查点和导航守卫不能被翻页笔绕过。

### 2.3 非目标

- 不把编辑器建设成完整的 Microsoft Word、PowerPoint、OneNote 或通用网页 IDE。
- 不在流式长文第一阶段支持任意位置文本容器、复杂桌面出版或多人实时协作。
- 不在空间画布第一阶段建设通用 3D 建模、材质、灯光、骨骼动画或物理编辑器。
- 不承诺把任意互动组件无损转换成 PPTX、DOCX 或 PDF 中的原生互动对象。
- 不在没有真实需求和质量证据前同时实现二维与三维空间编辑。
- 不为混合工程承诺整课 PPTX 或整课 DOCX 的无损导出。
- 不在内部正式版 1.0 中建设 Flow、Spatial 2D 或混合工程。
- 不保留 Project V1–V7、Runtime API 1 或 Component API 1–3 的主程序兼容层。
- 不在正式版 1.0 中通过 WebHID 识别、绑定或管理具体翻页笔设备，也不承诺拦截浏览器保留的所有厂商长按快捷键。
- 不在本轮处理许可证调整、代码签名、公开安装器、自动更新或外部分发渠道。
- 不在 1.0 中建设在线组件市场、Git 客户端、云同步、多人协作、题库成绩系统或编辑器内置 AI。

## 3. 表面类型与选择规则

用户界面可使用“模式”一词，但工程模型应使用明确的表面类型。

| 用户名称 | 建议内部类型 | 主要内容 | 编辑空间 | 主要质量目标 |
| --- | --- | --- | --- | --- |
| 幻灯片 | `slide` | 演示、逐幕讲授、互动实验、测验 | 固定 1280×720 | 单帧构图、投影可读、互动节奏、PPTX 映射 |
| 长文本 | `flow` | 讲义、教程、阅读材料、知识库、研究笔记 | 固定/最大宽度、无限纵向 | 结构、排版、阅读流、检索、分页、DOCX 映射 |
| 空间画布 | `spatial-2d` | 概念图、关系网、时间线、探索式系统 | 无限二维世界坐标 | 定向、语义缩放、镜头路径、空间关系、性能 |
| 混合课件 | 工程能力 | 多种表面的课程组合 | 多表面序列 | 跨表面连续性、导航、主题、状态、共同导出 |

### 3.1 默认路由

生成或新建课件前必须先判断内容意图：

```text
需要一屏一屏讲授、投影或交付 PPTX
  → 幻灯片

需要连续阅读、目录、章节和较长正文
  → 流式长文

理解依赖空间位置、关系、缩放或漫游
  → 空间画布

同一课程包含两种以上强需求
  → 混合工程，由每个表面选择自身类型
```

不得仅因当前编辑器只支持幻灯片，就把长文拆成大量文字幻灯片；也不得仅因空间画布自由度高，就把普通讲义或演示强行放进无限画布。

## 4. 交付与兼容导出矩阵

| 表面/工程 | `.h5lesson` | 单 HTML | 网页包 | PDF | PPTX | DOCX |
| --- | --- | --- | --- | --- | --- | --- |
| 纯幻灯片 | 作者态 | 原生互动 | 原生互动 | 每场景稳定帧 | 原生节点 + 复杂内容快照 | 不提供 |
| 纯流式长文 | 作者态 | 原生阅读与互动 | 原生阅读与互动 | 排版分页 | 不提供 | 结构化块映射 + 组件静态后备 |
| 纯空间二维 | 作者态 | 原生平移、缩放与互动 | 原生平移、缩放与互动 | 总览 + 导出镜头帧 | 不提供 | 不提供 |
| 混合工程 | 作者态 | 原生多表面运行 | 原生多表面运行 | 共同静态导出 | 仅兼容表面选择性导出 | 仅兼容表面选择性导出 |

### 4.1 共同导出原则

- 单 HTML 与网页包始终是完整互动成品，不使用 PDF/PPTX/DOCX 的能力反向裁剪 HTML。
- 兼容导出必须列出原生保留、静态化、忽略和失败后备，不允许静默丢失。
- 互动组件在 PDF/PPTX/DOCX 中使用确定性捕获或作者后备；必要时增加标题、说明和返回 HTML 成品的链接信息。
- 流式长文中的折叠内容默认在 PDF/DOCX 中展开；后续可增加作者指定的导出展开状态，但不得依赖运行时临时状态。
- 空间画布的 PDF 不直接“打印无限世界”，而是输出作者确定的总览和镜头帧。
- 混合 PDF 必须使用明确的打印配置。是否统一页面尺寸或允许混合页尺寸，在实施前通过样例验证后决定。

## 5. 多表面工程模型方向（正式版 1.0 之后）

内部正式版 1.0 的 Project V8 仍只承载固定 `1280×720` 幻灯片、`scenes`、场景状态、全局层和场景导航。它首先用于建立无旧版兼容负担的稳定生产基线，不提前塞入尚未验证的 Flow 或 Spatial 字段。流式长文和空间画布不应作为 `SceneDocument` 上的大量可选字段继续叠加。

建议在 Flow 与 Spatial 原型通过后设计 Project V9 或更高版本的判别联合：

```ts
interface ProjectDocumentV9 {
  schemaVersion: 9
  id: string
  title: string
  createdAt: string
  updatedAt: string
  surfaces: SurfaceDocument[]
  assets: Record<string, AssetMeta>
  componentPackages: Record<string, EmbeddedComponentPackageMeta>
  theme: ProjectTheme
  media: ProjectMediaSettings
  navigation: ProjectNavigation
}

type SurfaceDocument =
  | SlideSurfaceDocument
  | FlowSurfaceDocument
  | Spatial2DSurfaceDocument
```

以上仅是架构方向，不是最终 Schema。正式 Schema 必须在多个表面的最小原型和导出样例得到验证后冻结，且不得为了预留未来能力污染 Project V8。

### 5.1 共用能力

以下能力应归属于工程或通用宿主：

- 工程 ID、标题、时间和版本；
- 素材、组件包和离线资源解析；
- 主题、字体与基础设计令牌；
- 声音、视频和媒体策略；
- 课程级纯数据状态；
- 跨表面导航、目录和深链接；
- 统一发布、诊断、错误隔离和可信代码边界；
- 单 HTML、网页包和共同 PDF 的编排。

### 5.2 表面专属能力

| 能力 | Slide | Flow | Spatial 2D |
| --- | --- | --- | --- |
| 几何模型 | 1280×720 节点坐标 | 块顺序与流式布局 | 世界坐标与相机变换 |
| 稳定状态 | 命名场景状态 | 块内容/折叠与阅读状态 | 镜头书签、图层与空间状态 |
| 编辑器 | Player + 透明选择层 | DOM 块编辑器 | 空间场景图 + 相机工具 |
| 运行时 | 场景/全局运行时与组件 | 默认无表面运行时，允许嵌入组件 | 表面互动、空间节点与组件 |
| 缩略图 | 指定稳定状态 | 封面或首屏摘要 | 指定镜头或总览 |
| 静态捕获 | 单场景稳定帧 | 分页排版 | 总览与镜头帧 |

### 5.3 Player 方向

Player 应逐步从固定场景播放器演进为表面宿主：

```text
Course Player
  ├─ shared navigation / state / media / assets
  ├─ Slide Surface Host（复用现有 Player）
  ├─ Flow Surface Host（DOM 流式阅读）
  └─ Spatial 2D Surface Host（世界坐标与相机）
```

各宿主只实现自身语义，不要求用同一种渲染器统一所有表面。跨表面跳转必须保持课程状态，并明确重播当前表面、重开课程和返回目录的行为。

### 5.4 兼容与迁移

- Project V8 是内部正式版 1.0 的第一个受支持工程格式；Project V1–V7 不进入正式版主程序。
- 开始断代前必须把当前 1.7.0 工作区整理为可恢复的提交与归档标签。旧工程如未来确有恢复价值，应使用归档版本或另行制作离线转换工具，不把迁移链重新塞回正式版。
- Project V8 仍使用现有 Slide 语义；若未来进入 Project V9 多表面模型，只提供真实生产基线 V8 → V9 的显式迁移。
- PublishedLesson V1 在发布语义不变时继续服务 Project V8；只有多表面发布或边界确实变化时才设计 PublishedLesson V2。
- 多表面版本必须使用正式版 1.0 的两个已接受课例、Runtime API 2 和 Component API 4 做回归，不再维护旧运行时和旧组件协议。

## 6. 内部正式版 1.0 收敛方案

### 6.1 版本与协议边界

| 层级 | 内部原型现状 | 正式版 1.0 决策 |
| --- | --- | --- |
| 应用版本 | `1.7.0`，未公开分发 | 重置为 `1.0.0`，明确此前版本为内部原型线 |
| 作者态工程 | Project V1–V7 逐级迁移 | 只读写 Project V8；旧版本明确拒绝 |
| 自由运行时 | Runtime API 1/2 | 仅 Runtime API 2 |
| 互动组件 | Component API 1–4 | 仅 Component API 4 |
| 发布格式 | PublishedLesson V1 | 语义未变时继续使用 V1 |
| 课件表面 | Slide | 仍只生产化 Slide；多表面后移 |

断代不是直接删除。开始前必须：

1. 整理当前未提交和未跟踪修改，形成可重建的 1.7.0 基线提交；
2. 创建清晰的归档标签并记录启动、构建、打开旧工程的方法；
3. 建立“保留、迁出、删除、改写”的文件级清单；
4. 从旧样例中提取仍保护当前能力的断言，再删除协议兼容夹具；
5. 为旧工程、旧运行时和旧组件提供明确错误信息，禁止部分解析后产生损坏工程。

### 6.2 仓库边界

正式版 1.0 的目标是采用三个职责清晰的源码边界；下图是完成 W1 课例迁移后的目标布局，不表示 S6 已经物理建立 `courseware-cases`：

```text
courseware-editor/
  src/                         # 编辑器、Player、Schema、导出器
  tests/fixtures/current/      # 仅当前协议的最小综合夹具
  docs/                        # 当前能力与开发规范

courseware-components/
  catalog.json
  common/
  subjects/math/
  subjects/physics/
  experimental/

courseware-cases/
  high-school/math/<case-id>/
    orchestration/
    project/
    components/                # 课例专属，尚未晋升
    evidence/
    exports/
```

- `courseware-editor` 的目标职责不保存正式课例、领域组件源码和可重建成品，只保留测试产品合同所需的最小夹具。S6 期间现有旗舰/失败课例仍作为历史与诊断资产暂存，不构成当前工作流成功证据，也不新增正式课例。
- `courseware-components` 保存可复用组件的权威源码、构建结果、缩略图、版本、哈希、适用范围和质量状态。
- `courseware-cases` 将保存教学设计、教学内容规格、教学呈现脚本、实现追踪、课例专属代码、工程、证据与导出结果；其物理根目录在用户批准 W1 后建立。
- S6 只冻结三个边界的职责、目标布局和原子迁移清单；当前物理落地软件实际依赖的 `courseware-components`。W1 第一项再建立相邻 `courseware-cases` 并迁移历史课例，正式版 1.0 不建设远程 Git 管理、自动拉取或在线市场。

### 6.3 本地组件库与工程自包含

编辑器应支持配置和扫描以下组件来源：

- 随编辑器提供的最小内置稳定库；
- 团队共享本地组件库；
- 用户个人组件库；
- 当前课例仓库内的项目专属组件目录。

组件目录条目至少包含：

- 稳定 ID、版本、名称、学科、学段、标签和说明；
- Component API、支持作用域、渲染模式和入口包路径；
- 缩略图、构建产物哈希、来源/维护者说明和可信状态；
- `experimental | candidate | stable | deprecated` 质量状态；
- 已通过课例和验证时间。

作者使用组件时，编辑器必须执行：发现 → 预览 → 信任确认 → 校验 → 复制 → 锁定版本与哈希 → 嵌入工程。库中出现新版本时只提示，不自动升级已有工程。`.h5lesson`、单 HTML 和网页包不得保存指向外部组件目录的运行时引用。

课例专属组件默认保持 `experimental`。至少在第二个独立课例中验证确有重复职责、可配置边界和生命周期价值后，才评估晋升为共享组件；一次性复杂行为继续优先使用场景或全局运行时。

### 6.4 Skill 中文化

Project V8 与软件仓库边界已经冻结。两个课件 Skill 的中文化和 V8 实现 Skill 重做仍属于获批后的 W1；在此之前只维护当前编排入口，不改写归档 V7 Skill 冒充新实现。

中文化规则：

- 目录名和 frontmatter `name` 保持稳定英文；
- `description` 使用简洁中英双语，优先覆盖中文教材、课标、题目、教学设计和实现交接表达；
- `SKILL.md` 正文、参考规范、默认提示、错误信息和人类可审阅说明改为中文；
- 命令、路径、Schema 字段、事件名、状态值、哈希字段和代码标识保持英文；
- 不是逐句翻译，而是按中文技术写作习惯重写并消除中英混杂歧义；
- 仓库 `.agents/skills/` 继续作为权威源，启动脚本继续幂等同步到用户 Skill 目录。

发布前使用冻结用例做前后对照：至少 10 条中文触发、3 条英文触发、越级实现负例、缺少批准负例、上下文压缩恢复、错误内容回退、公式扫描和实现追踪。触发率、门禁纪律、制品合法性或拒绝行为发生退化时不得替换权威版本。

### 6.5 教师全局控制器运行时拖动

正式版 1.0 的 `TeacherControllerNode` 在互动 Player 中默认启用运行时拖动；这是控制器宿主的统一行为，不要求作者为按钮区、标题区或收起态另行配置拖拽区域：

- 作者态 `x/y` 是初始位置；成品拖动只保存会话临时偏移，不写回 Project；
- 切换场景、返回和重播保留位置；重开课程、刷新或重新打开 HTML 恢复作者位置；
- 不设置专用拖拽把手；标题、背景、按钮和收起胶囊等整个可见控制器都可发起同一套点击/拖动候选手势；
- `pointerdown` 只记录起点和原始命中目标，不立即执行按钮。释放前未超过视觉位移阈值时才按原目标执行点击；一旦超过阈值就转入拖动，并永久取消本次按钮、收起或展开动作；
- 位移阈值按屏幕像素和输入类型判定，拖动坐标再换算到 1280×720 逻辑画布，避免画布缩放改变点击手感；进入拖动后捕获指针并阻止事件传给底层课件；
- 展开面板与收起胶囊使用完全相同的手势语义，收起胶囊轻点展开、拖动则移动，收起后支持贴边停放；
- 位置限制在 1280×720 逻辑画布可见范围，缩放、全屏和不同窗口尺寸使用同一逻辑坐标；
- 按钮继续使用 Enter/Space 激活，控制器获得焦点时使用 Alt+方向键等不冲突的组合完成键盘移动，并提供焦点样式和 `aria-live` 反馈；触控拖动不得触发页面滚动或底层课件操作；
- `pointercancel`、失焦或宿主销毁必须取消当前候选手势，不得补发点击；
- 运行时偏移独立于命名状态更新，状态切换不得把控制器跳回作者位置；
- PDF/PPTX 继续默认省略控制器，显式保留时只输出作者稳定位置的静态帧。

### 6.6 翻页笔与演示输入层

翻页笔通常以标准键盘 HID 输出按键。正式版 1.0 不通过 WebHID 请求设备权限或维护厂商适配表，而是在 Player 中建立统一 `PresenterInput`：

```text
KeyboardEvent
  → 按键绑定与冲突过滤
  → presenter.next / presenter.previous
  → 简单场景导航或作者规则
  → 导航守卫
  → 执行结果与阻断反馈
```

默认按键与范围：

| 命令 | 默认按键 | 说明 |
| --- | --- | --- |
| `presenter.next` | `PageDown` | 标准翻页笔前进键；Project V8 新工程默认启用 |
| `presenter.previous` | `PageUp` | 标准翻页笔后退键；Project V8 新工程默认启用 |
| 键盘下一场景 | `ArrowRight` | 继续由独立的键盘方向键设置控制 |
| 键盘上一场景 | `ArrowLeft` | 继续由独立的键盘方向键设置控制 |

[Logitech R400/R700/R800 官方按键说明](https://support.logi.com/hc/en-za/articles/360023345333-HID-and-keyboard-codes-for-Logitech-R400-R700-and-R800-presenters)明确使用 `PageDown/PageUp` 发送前进/后退；这两个值也是 [W3C UI Events](https://www.w3.org/TR/uievents-key/) 定义的标准页面导航键。第一版以标准 `KeyboardEvent.key` 为权威，不使用 `keyCode`，也不假定翻页笔型号。

Project V8 的演示输入至少支持两种推进策略：

- `scene-navigation`：适用于普通线性幻灯片。`presenter.next/previous` 请求相邻场景，但必须经过现有导航守卫；被阻止时保持当前画面并显示原因。
- `authored-command`：适用于预测、作答、反馈、教师检查点等互动课件。按键只产生可在“互动与动画”中配置的 `presenter.command` 触发器，由作者规则决定切换命名状态、播放动作或进入场景；没有隐式切幕后备。

不能根据“某个控制器按钮当前是否可见”反推翻页笔行为。视觉按钮和演示输入应调用同一语义动作或规则，检查点必须由声明式规则或导航守卫表达。Skill 和工程检查应诊断“翻页笔已启用、存在受控推进，但没有规则或守卫”的潜在绕过风险。

输入冲突规则：

- 仅在互动预览、整课预览、单 HTML 和网页包中启用；编辑、authoring 和 capture 模式不响应；
- 焦点位于 `input`、`textarea`、`select`、`contenteditable`、滑块或显式声明接管键盘的组件时不拦截；
- `event.defaultPrevented`、输入法组合、未登记组合键和打开的模态界面优先于全局演示输入；
- 只处理 `keydown`；默认忽略 `event.repeat` 并进行短时间去重，避免长按一次跨过多个场景；
- 只有命令被识别且准备处理时才 `preventDefault()`；首页、末页或守卫阻断必须在教师控制器或轻量提示中给出反馈；
- PageUp/PageDown 与方向键保持独立开关，避免为了支持翻页笔扩大普通键盘对互动组件的干扰。

编辑器提供“测试翻页笔”入口：用户开始检测后分别按前进和后退，界面显示收到的 `key`、`code`、修饰键和重复状态。标准按键自动识别；其他能被浏览器正常交付的按键可作为项目额外绑定。额外绑定随 Project 和 PublishedLesson 保存，默认 PageUp/PageDown 始终保留，已有工程不因更换翻页笔而失效。

第一版只承诺前进、后退、按键检测和自定义附加绑定。黑屏、启动放映、退出放映和长按快进属于后续增强；不得默认映射 `F5`，因为它在浏览器中可能刷新页面并丢失会话状态。[WebHID](https://wicg.github.io/webhid/index.html) 仍可作为未来特殊硬件研究，不进入离线 HTML 的基础依赖。

### 6.7 公式与导出质量

正式版 1.0 必须建立语义化公式最低能力，至少覆盖：

- 纵向分式；
- 上标与下标；
- 根式；
- 括号、绝对值和常见关系/运算符；
- 编辑画布、Player、单 HTML、PDF 和 PPTX 的可解释一致性。

不得把 `a/b`、Unicode 斜线分数或倾斜文本当作正式纵向分式。公式应保存结构化内容或可验证的语义源，而不是只保存一张不可追踪图片。PPTX 无法原生保留时可以静态化，但必须在导出报告中说明。

四种成品格式现在共用同一 Export Preflight 问题模型，并按目标格式分别生成报告：

- 文字溢出、裁切和字号过小；
- 系统字体缺失与可能发生的换行差异；
- 公式降级或静态化；
- 组件/运行时捕获失败和所用后备；
- PDF/PPTX 空白页、越界对象和页面数量异常；
- HTML 外部网络请求和未嵌入资源；
- 控制器、视频、声音和互动在静态格式中的预期差异。

每项使用稳定 code、`error/warning/info` 严重度、目标格式和可选 scene/state/node/path 定位。确定性错误阻断导出；警告与说明须展示但允许人工确认继续。对比度、视觉密度、图片硬边和控制器遮挡属于启发式风险，不得伪装成错误。报告可保存为 JSON；它与长期 Project Health、异常诊断日志各自独立。

### 6.8 最小作者评审辅助

本轮已选择四项不建立第二运行时的最小能力：

- `ProjectDocument.designTokens`：最多承载稳定 ID、名称、CSS 字体值或颜色值；只提供机器可读词汇，不保存叙述性美术方向，不自动绑定或重写节点；
- `ImageNode.safeAreas`：稳定 ID 与归一化几何，只在作者画布显示覆盖层；不改变裁剪/焦点，不进入 Player、缩略图或导出画面；
- 信息释放：按现有 scene/state/interaction 近似计算初始可见、分步显示和未连通隐藏；外部事件只按可能发生处理，不模拟授课；
- 视觉密度：按节点、字符、累计包围盒面积和明显重叠生成 0–100 启发式分数，不判定教学或美术质量。

动态过程多帧捕获仍是研究项。当前 `capture.waitUntil()` + `prepareCapture()` 已保证单个确定帧；没有真实课例和终端语义证据前，不增加时间线、多帧 hook 或第二份捕获状态模型。

### 6.9 内部正式版发布边界

内部正式版 1.0 可以继续通过受控源码入口或内部未签名构建运行，不要求公开安装器、自动更新、许可证切换、代码签名或外部分发渠道。正式版判定仍必须包含：

- 在一台干净的内部 Windows 环境中按文档成功启动；
- 新建、编辑、保存、重开、恢复、预览和撤销/重做正常；
- 单 HTML、网页包、PDF 和 PPTX 离线交付通过；
- 标准 PageUp/PageDown 翻页笔在整课预览、单 HTML 和网页包中可用，作者规则与导航守卫行为一致；
- 本地组件库发现、导入、锁定、嵌入和更新提示通过；
- 可导出的本地诊断信息足以定位启动、工程、组件和导出失败；
- 两个全新冷启动课例达到人工 `accepted`；
- 文档不再把旧协议、旧样例或未来多表面描述为正式版现有能力。

### 6.10 AI-native 编辑器基建增量（2026-08-12）

本增量属于软件本体收敛，不是工作流、Skill 或课例开发。本节保留实施结论，详细证据见 [AI-native 编辑器基建验证记录](docs/reviews/AI_NATIVE_EDITOR_FOUNDATION_VERIFICATION_20260812.md)。目标是减少 AI 理解工程时的歧义并消除数据损坏入口，而不是把编辑器扩张为通用编程平台：

- P0 在不改 Git 暂存/提交状态的前提下冻结 tracked patch、untracked 归档、文件清单、HEAD/状态与九组件哈希/发布阻断；
- P1 为 Project V8 Zod 输出与 `ProjectDocument` 建立双向编译期门禁，并用独立 `tsconfig.e2e.json` 把 Playwright/Electron 验收代码纳入 `npm run typecheck`；两个方向的临时突变均被合同捕获，恢复后全量复验通过；
- P2 用一份带位置、区分 direct/conservative 的素材引用图统一删除保护、工程检查、归档和网页发布投影；单项素材操作进入引用/元数据/字节同一撤销事务，诊断 code 收敛为类型化注册表；
- P3 已删除不可达的原生节点 Phaser 视觉适配链、重复组件文字编辑浮层和无消费者布局/预览代码；隔离 Player 保持唯一视觉真相，透明 Phaser 只保留 `ProxyNodeAdapter` 几何/命中路径；
- P4 生成 5,079 bytes 的 `artifacts/ai-capabilities/index.json` 与按需分域契约，读取 Schema、协议常量、诊断注册表和受校验 catalog；确定性、过期/超限、九包哈希和目录降级夹具均已通过。该索引不代表内置 AI 或 V8 实现 Skill，九组件仍是 `experimental` 并保留许可、来源和维护人阻断；
- P5 只形成 [声明式课程状态与导航守卫 RFC](docs/reviews/DECLARATIVE_COURSE_STATE_RFC_20260812.md) 与测试目录内存原型。它没有进入 Project Schema、InteractionEngine、作者 UI 或导出协议；Project V8/V9、capture 冻结和导航守卫组合仍需人类决定。

P1–P4 的类型、123 文件/777 项 Vitest、生产构建、组件目录校验、能力清单 `--check` 和始终隐藏的 Electron E2E 27/27 均已回填，本增量签发为 `engineering candidate`。这不能解除组件发布阻断，也不能跳过 W1/W2 的工作流、Skill 和真实课例结果验收。

## 7. 各表面的创作与实现工作流

### 7.1 幻灯片模式

幻灯片不是低质量模式，也不以“反 PPT”为目标。质量目标是避免无意识的统一模板套壳，同时保持投影、逐幕讲授和 PPTX 兼容优势。

创作顺序：

1. 明确本幕教学动作、学生操作和稳定结果；
2. 设计 1280×720 单帧构图及进入/退出节奏；
3. 区分原生节点、命名状态、声明式交互、运行时和组件；
4. 先完成代表性的核心互动和关键帧；
5. 真实截图、操作和 PPTX 导出检查通过后再扩展全课；
6. 分别报告互动 HTML 质量与 PPTX 静态兼容质量。

模式专属门禁：

- 投影距离下标题、正文、图表和操作目标可读；
- 稳定状态可形成有意义的单页 PDF/PPTX；
- 连续页面的重复布局是有意设计，而不是生成器默认套壳；
- 原生对象在 PPTX 中保持可编辑，复杂视觉只在必要处静态化；
- PPTX 静态化不反向削弱 HTML 互动体验。

### 7.2 流式长文模式

第一阶段采用结构化块模型，而不是把现有 `TextNode` 高度不断增大。

建议首版块类型：

- 标题与多级标题；
- 段落、引用、列表和分隔线；
- 图片、视频、音频和图注；
- 表格、公式与代码块；
- 提示、警告、例题和结论块；
- 可折叠章节；
- 互动组件嵌入块；
- 页内目录和锚点。

编辑体验：

- 内容区域使用固定或最大阅读宽度，页面高度随内容增长；
- 默认直接输入，支持块插入、块级拖动、复制、删除和层级调整；
- 标题自动进入大纲，可通过大纲快速导航；
- 允许局部通栏媒体或互动组件，但正文保持稳定阅读宽度；
- Undo/Redo、复制粘贴、保存恢复和搜索必须以块为稳定单位；
- 首版不提供整页自由运行时；复杂互动通过受控组件嵌入；
- 组件必须提供 HTML 互动实现和 PDF/DOCX 确定性静态后备。

DOCX 映射基线：

| Flow 内容 | DOCX 结果 |
| --- | --- |
| 标题 | Word 标题样式与大纲级别 |
| 段落/列表/引用 | 原生段落、编号和样式 |
| 图片与图注 | 原生图片和题注 |
| 表格 | 可编辑原生表格，超出能力时静态化并警告 |
| 公式 | 优先原生公式或标准可编辑表示，失败时使用图片后备 |
| 折叠章节 | 导出完整内容并保留标题层级，不承诺运行时折叠状态 |
| 互动组件 | 确定帧图片 + 名称/说明 + 可选 HTML 成品引用 |

PDF 排版必须处理分页、孤行寡行、标题与正文保持、表格和组件跨页、页眉页脚、目录及字体嵌入策略。不能把整个长页面截图后切片冒充文档排版。

### 7.3 空间二维模式

空间二维表面至少需要：

- 无限或大范围世界坐标；
- 平移、缩放、框选、对齐和图层；
- 相机当前位置、主页视图和一键回到内容；
- 镜头书签、命名帧和可选教学播放路径；
- 小地图、方向提示和当前位置反馈；
- 基于缩放级别的语义显示策略；
- 复用文字、图片、图形、视频和受控组件；
- 对大量节点的视口裁剪、分级加载和性能诊断；
- HTML 中真实保留探索、缩放和镜头调度；
- PDF 中输出总览与作者选定镜头帧。

首版非目标：

- 通用三维对象编辑；
- 任意网页嵌入；
- 复杂物理世界；
- 无限分辨率打印；
- 没有镜头或边界定义的自动 PDF 排版。

### 7.4 三维内容的近期策略

近期继续使用现有 Runtime API 2 或 Component API 4 承载 3D：

- Three.js、GLB 和相关依赖只随具体内容离线打包；
- 组件/运行时负责相机、灯光、材质、交互、捕获和 GPU 释放；
- 稳定文字、反馈和课程状态继续接入工程模型；
- PDF/PPTX/DOCX 使用确定镜头捕获；
- 只有在多个真实课件反复需要通用 3D 编辑后，才启动三维表面 RFC。

### 7.5 混合工程

混合工程按统一顺序组织不同表面，并提供：

- 统一课程目录；
- 前一项、后一项和返回目录；
- 跨表面稳定 ID 和深链接；
- 共享主题、素材、声音和课程状态；
- 从 Slide 进入 Flow、从 Flow 进入 Spatial 等显式过渡；
- 每个表面的独立重播/重置语义；
- 整课 HTML 预览；
- 共同 PDF 导出和逐项静态化报告。

混合模式不能成为掩盖设计不一致的容器。跨表面必须保持术语、视觉语言、导航位置和学习进度连续，同时允许每种表面使用自己的布局与互动优势。

## 8. 分阶段开发路线

### 里程碑 0：冻结内部原型基线与正式版合同

**状态：已完成。** 1.7.0 工作区已通过类型检查、641 项测试和桌面生产构建，并冻结为提交 `af5908d` 与归档标签 `internal-prototype-1.7.0`；技术合同和文件级处置清单见 [里程碑 0 冻结记录](docs/INTERNAL_1_0_MILESTONE_0.md)。

目标：把“哪些保留、哪些迁出、哪些删除、正式版支持什么”变成可验证合同，避免在大规模删除中丢失仍有价值的能力。

主要工作：

- 整理当前改动，形成可构建、可测试的内部 1.7.0 基线提交和归档标签；
- 记录 App 1.0.0、Project V8、Runtime API 2、Component API 4 和 PublishedLesson 的版本决定；
- 形成旧 Schema、迁移、协议适配、示例、脚本、测试和文档的文件级处置清单；
- 冻结编辑器、组件库、课例库的仓库职责和本地目录发现合同；
- 冻结教师控制器拖动、翻页笔演示输入、公式语义、导出预检和内部发布边界；
- 保留管线状态与成品结果状态分离的验收协议。

验收：

- 归档版本可以按文档重建和打开旧工程；
- 删除清单中的每项都有保留、迁出、替换或删除理由；
- 未来多表面能力明确不属于正式版 1.0；
- 尚无截图、录像和人工确认时，结果状态不能标记为 `accepted`。

### 里程碑 1：Project V8 与协议断代

**状态：已完成。** 新建、保存、打开、恢复、Player Payload、运行时和组件生产路径只接受 Project V8、Runtime API 2 与 Component API 4；Project V1–V7、Runtime API 1、Component API 1–3 的迁移/宿主兼容分支、历史夹具和专属测试已移出当前主干。旧输入在解析或导入边界得到明确“不受支持”结果，不会部分载入或静默迁移。历史 S6 证据保留在 2026-08-11 报告，当前增量基线以 2026-08-12 的 123 文件/777 项 Vitest 与隐藏 E2E 27/27 为准。

目标：在不重写已稳定 Slide Player 的前提下建立只支持当前协议的干净工程核心。

主要工作：

- 建立只承载 Slide 的 Project V8 Schema、类型、创建、保存、发布和诊断；
- 移除 Project V1–V7 解析与逐级迁移链；
- 移除 Runtime API 1 和 Component API 1–3 的类型、注册、适配与测试路径；
- 旧工程、旧运行时和旧组件使用明确“不受支持”诊断，不允许部分载入；
- 删除 `runtime-v3-complete` 等兼容夹具，把仍保护当前能力的断言重写为 API 2/API 4 当前夹具；
- 更新 README、用户指南、创作规范、运行时/组件规范、构建脚本和测试命名。

验收：

- 新建、保存和导出的作者态工程全部是 Project V8；
- 主程序源码、文档、测试和示例不再宣称支持旧协议；
- Runtime API 2、Component API 4、统一画布、命名状态、全局层和四种导出保持通过；
- 旧格式失败可理解、可定位且不产生被标记为已保存的半损坏工程。

### 里程碑 2：仓库职责冻结与本地组件库

**状态：软件实现与作者入口重构均已通过当前 `engineering candidate` 自动化基线。** 相邻 `courseware-components` 目录、`catalog.json` 扫描/搜索/信任/哈希/锁定/显式更新链路和首批九个组件已经落地，九组件 V8 矩阵通过 2/2，并覆盖九包、225 次压力导航、9 页 PDF 与 9 张 PPTX。专业模式现以独立“组件”页承载全尺寸内置库、外部批量导入和合并管理后的工程组件；内置库按 catalog 动态生成通用/学科、学段和用途筛选，多选只加入包、不自动放置实例。catalog 的九项记录各登记 5 个验证 case，并只移除了 `current-v8-full-matrix-unverified`；朗读标注、拼音标注及七个视觉容器仍全部是 `experimental`，许可、素材来源和维护人发布阻断继续有效，不得标记为 `candidate` 或 `stable`。`courseware-cases` 在本阶段只冻结职责、目标布局和迁移清单，不物理建库或移动历史课例。

目标：让编辑器核心、可复用组件和课例内容各自拥有清晰生命周期，同时维持工程自包含和离线交付。

主要工作：

- 建立相邻 `courseware-components` 仓库；冻结 `courseware-cases` 的职责、目标布局和旗舰/失败课例原子迁移清单；
- 把领域组件迁出编辑器核心；课例档案、课例构建脚本、证据和导出物在用户批准 W1 后作为第一项工作原子迁移；
- 软件生产路径与当前协议验证只依赖 Project V8/API 2/API 4 综合夹具，不依赖尚未建立的课例仓库；
- 定义本地组件 `catalog.json`、状态、元数据、哈希和缩略图合同；
- 增加内置、团队、个人和项目本地组件目录配置与扫描；
- 增加组件目录浏览、搜索、预览、信任确认、导入、版本锁定和更新提示；
- 导入后继续嵌入 `.h5lesson`，发布物不依赖库路径；
- 实现 `experimental → candidate → stable → deprecated` 晋升治理。

验收：

- 移动或断开组件库后，已保存工程和导出的 HTML 仍完整运行；
- 同 ID/版本/哈希冲突、损坏包和不受支持 API 都有清晰诊断；
- 课例专属组件不会因单个成功案例自动进入共享稳定库；
- 核心仓库不再混入领域组件源码；S6 期间暂存的历史旗舰/失败课例有明确的 W1 迁移清单，不被宣称为当前正式课例或工作流成功证据；
- `courseware-cases` 物理根目录及历史课例迁移不是 S6 软件工程基线的阻断项。

### 里程碑 3：正式版高价值质量能力

**状态：软件实现与作者体验增量已通过当前自动化基线，签发结果为 `engineering candidate`。** 教师控制器会话拖动、PresenterInput、受限线性公式编辑、四格式 Export Preflight、designTokens、安全区、只读信息释放、视觉密度、媒体/组件批量导入和连续插入已经进入当前代码。2026-08-12 同一工作树通过 123 文件/777 项 Vitest、生产构建、组件/能力清单校验与始终隐藏的 Electron E2E 27/27；九组件和 FormulaNode/着重号跨表面制品仍有可追溯证据。当前公式子集不等于任意 LaTeX 或 Word/MathLive 完整体验；真实教师任务、中文输入法、读屏、100 卡片 UI 性能、三仓物理拆分、干净 Windows 启动和真实翻页硬件人工冒烟仍未完成，不得宣称 `accepted` 或发布就绪。

目标：解决内部实际授课和数学内容中已经观察到的高价值问题。

主要工作：

- 实现教师全局控制器整面可拖、阈值区分点击/拖动、收起贴边、逻辑画布限位、键盘等价操作和会话状态；
- 把现有左右键翻页重构为统一 `PresenterInput`，默认支持 PageDown/PageUp，并保持方向键独立开关；
- 增加 `scene-navigation` 与 `authored-command` 两种推进策略、`presenter.command` 规则触发器、按键检测和项目附加绑定；
- 所有演示导航统一经过规则或导航守卫，并为阻断、首页和末页提供教师可见反馈；
- 建立语义化公式最低能力，覆盖纵向分式、上下标、根式和常见数学符号；
- 保证公式在编辑、Player、HTML、PDF 和 PPTX 中使用明确的原生或静态后备；
- 增加文字、字体、公式、组件捕获、空白页、越界、外部资源和静态差异的导出预检；
- 增加最小字体/色板 `designTokens` 与图片 `safeAreas`，二者都保持作者态、机器可读和非自动改写；
- 在 Project Health 中增加只读信息释放与视觉密度概览，不新建平行运行时或教学判断器；
- 增加可导出的本地诊断信息；
- 重新验证保存、自动恢复、撤销/重做、媒体、离线运行和局部失败隔离。

验收：

- 控制器在鼠标、触控、键盘、缩放和全屏环境下行为稳定；轻点按钮正常执行，超过阈值后只拖动且不误触按钮或底层课件；
- 切幕与重播保持临时位置，重开恢复作者位置，静态导出使用作者稳定状态；
- 标准 PageDown/PageUp 翻页笔在整课预览、单 HTML 和网页包中即插即用，长按不连续误翻页；
- `scene-navigation` 遵守导航守卫，`authored-command` 只执行显式规则且没有隐式切幕后备；输入控件、滑块、组件键盘接管和模态界面不被抢键；
- 分式不再以斜线或倾斜文本冒充，公式降级均进入预检报告；
- PDF/PPTX 页数、空白、越界、可编辑对象和静态快照有确定检查结果。
- 四种目标的预检均区分 error/warning/info，错误阻断、其余可确认继续，定位和 JSON 保存可用；信息释放、视觉密度、安全区及设计 Token 不产生隐式节点修改。

### 里程碑 4：W1/W2 工作流准备与冷启动前向验证

依赖：Project V8、仓库边界、公式合同和实现规范冻结。

目标：让人类能够直接理解、审阅和修改通用创作编排与实现 Skill，并证明中文化没有破坏触发或门禁。

主要工作：

- W1 获用户批准后，第一项先建立相邻 `courseware-cases`，再按原状态原子迁移旗舰课例、失败课例及其脚本、证据和导出物；失败课例继续保持 `pipeline: passed / outcome: rejected`，二者都不计入冷启动验收；
- 中文重写两个 `SKILL.md`、六份参考规范、默认提示和错误说明；
- 保留稳定英文 ID、技术字段和中英双语触发描述；
- 更新实现 Skill 只面向 Project V8/API 2/API 4；
- 通用编排吸收语文学科交接中可跨学科复用的教学事件、代表性样机门禁、证据失效重验、信息逐步释放和学生/教师双视角；不建立第二套并列总编排器；
- 语文扩展只保存学科特有的诵读、标注、文本证据和文学性呈现规则；《春》阶段 8 未通过的样机及阶段 9 以后成果禁止复用，课例参数和具体视觉数值不得泛化为软件或通用规则；
- 保持仓库权威源与启动脚本幂等安装；
- 运行冻结的中英文触发、批准、越级拒绝、恢复、公式和追踪测试；
- W2 从全新主题启动一个高中数学课例和一个非《春》的语文文学性课例；两者使用不同互动机制，且历史旗舰、失败课例和语文失败样机均不计入冷启动验收。

验收：

- 中文 Skill 的触发、门禁、制品哈希、恢复和拒绝行为不低于原基线；
- 两个课例均从教学设计、内容规格、呈现脚本、视觉方向和 implementation-ready 交接开始，不依赖旧聊天摘要；
- 实现逐项追踪获批 `BEAT-*`，不擅自简化教学容量、难度或互动；
- 至少一个课例验证正式数学公式，两个课例都具备真实 HTML、截图、录屏和静态导出证据。

### 里程碑 5：W3 内部正式版 1.0 验收

目标：把当前能力作为内部正式产品接受，而不是只完成代码和文档。

验收门禁：

- 类型检查、单元、集成、Electron E2E、Player/Renderer/Electron 生产构建全部通过；
- 一台干净内部 Windows 环境可按文档启动，无需外部分发渠道；
- Project V8 新建、编辑、保存、恢复、重开、预览、HTML、网页包、PDF 和 PPTX 全链路通过；
- 本地组件库和工程自包含边界通过断开目录、移动工程和离线打开验证；
- 两个冷启动课例的管线状态通过，成品结果经人工审阅达到 `accepted`；
- README、用户指南、开发规范、Skill 和示例只描述正式版真实能力；
- 许可证调整、代码签名、公开安装器、自动更新和外部分发明确列为本轮非目标，不阻塞内部 1.0。

### 里程碑 6：Flow 独立原型

只有内部正式版 1.0 达到上述门禁后才启动。原型验证结构化块、固定/最大阅读宽度、无限纵向布局、大纲、折叠、组件嵌入、HTML 阅读、A4 PDF 分页和 DOCX 结构映射。不得通过长截图切片冒充文档排版。

### 里程碑 7：Project V9 与 Slide + Flow 生产化

依赖：Flow 独立原型达到 `art candidate`。冻结 Project V9 判别联合、Surface Host 和 PublishedLesson 是否升级；只迁移真实生产基线 Project V8，不恢复 Project V1–V7 兼容。

### 里程碑 8：Slide + Flow 混合工程

依赖：两种表面分别生产可用。实现统一目录、导航、主题、媒体、课程状态、共同 HTML/PDF 和选择性 PPTX/DOCX，并明确不兼容表面的导出差异。

### 里程碑 9：Spatial 2D 原型、生产决策与三维复审

只有多表面宿主稳定后，才用确实依赖空间关系的课例验证世界坐标、相机、平移缩放、镜头书签、小地图、视口裁剪、HTML 探索与总览/镜头 PDF。只有观察到相对 Slide/Flow 的明确教学收益后才生产化；通用 3D 继续等待多个真实课例的直接编辑需求。

## 9. 共用质量与验收体系

### 9.1 两类状态必须分开

**管线状态**包括：

- 当前 Schema、旧格式明确拒绝、素材和引用；
- 保存、重开、恢复和撤销；
- Player、生命周期和离线运行；
- 导出、静态后备和错误隔离；
- 类型检查、单元测试、集成测试和端到端测试。

**结果状态**使用：

- `unusable`
- `placeholder`
- `engineering candidate`
- `art candidate`
- `accepted`

管线通过最多证明 `engineering candidate`。只有模式专属结果验收和实际证据通过后才能成为 `accepted`。

### 9.2 所有表面的共用结果门禁

- 教学目标、学习动作、反馈和结束条件明确；
- 内容选择的表面与教学意图匹配；
- 人工可见文字可编辑；
- 视觉层级、文字可读性和操作发现性可接受；
- 键盘、焦点、对比度、替代文本和缩放有明确处理；
- 动效、声音和媒体服务教学目标；
- 核心流程有截图和真实操作证据；
- HTML 与兼容导出的差异已列出；
- 未实现项和风险没有被“通过”掩盖。

### 9.3 模式专属证据

| 模式 | 最小证据 |
| --- | --- |
| Slide | 关键状态截图、核心互动短录屏、PDF 页面、PPTX 对象/快照检查 |
| Flow | 首屏与长文截图、目录/折叠操作、跨页 PDF、DOCX 重开与渲染检查 |
| Spatial 2D | 总览、多个缩放层级、镜头路径录屏、PDF 镜头页、性能记录 |
| Mixed | 跨表面导航录屏、状态连续性、共同 PDF、选择性导出报告 |

不得在没有对应证据文件时写死 `visualReview: passed` 或等价字段。

## 10. 测试与性能策略

正式版 1.0 的 Slide 基线至少覆盖：

- Project V8 正负例，以及 Project V1–V7、Runtime API 1、Component API 1–3 的明确拒绝；
- 本地组件目录扫描、搜索、信任、导入、锁定、嵌入、冲突和更新提示；
- 断开组件库、移动 `.h5lesson` 和离线打开后的工程自包含；
- 控制器全表面指针/触控/键盘拖动、点击与拖动阈值、按钮动作抑制、指针取消、缩放、全屏、限位、收起、切幕保持和重开复位；
- 翻页笔 PageDown/PageUp、方向键独立开关、项目附加绑定、`event.repeat`/去重、焦点与模态冲突、规则模式、导航守卫、边界反馈和销毁清理；
- 翻页笔在整课预览、单 HTML、网页包中的一致性，以及至少两类实际硬件/按键输出的人工冒烟；
- 纵向分式、上下标、根式在编辑、Player、HTML、PDF 和 PPTX 中的表现与降级报告；
- 导出预检对文字、字体、公式、捕获、空白页、越界、网络和静态差异的正反例；
- `designTokens` 与 `ImageNode.safeAreas` 的 Schema、编辑、撤销/重做、保存重开和“不会自动改节点/不会进入成品”负例；
- 信息释放可达性与视觉密度分数的确定性测试，并验证两者只读、启发式且不单独阻断导出；
- Playwright/Electron 默认主窗口与预览窗口始终隐藏、不调用 `show()`、不进入任务栏或抢焦点；只有显式可视调试脚本显示窗口；
- 两个冷启动课例的制品哈希、脚本追踪、离线输出和人工结果证据。

正式版 1.0 及未来每个新增表面共同覆盖：

- Schema 正负例与受支持版本迁移；
- 创建、编辑、复制、删除、撤销和重做；
- 保存、重开、恢复和损坏输入；
- 素材、组件和文字编辑；
- 本地预览、单 HTML 和网页包一致性；
- PDF 及表面专属办公格式导出；
- 字体、图片、视频和组件异步准备；
- 键盘、焦点、缩放和不同窗口尺寸；
- 大内容量压力与资源释放；
- 单个内容块/组件失败时的局部隔离。

性能预算应在原型阶段用真实课例测量后冻结，不在缺少基准时凭空设定。Slide、Flow 和 Spatial 2D 分别建立节点数、块数、文档长度、视口渲染、发布体积和捕获时间基线。

## 11. 主要风险与控制方式

| 风险 | 后果 | 控制方式 |
| --- | --- | --- |
| 未归档就删除旧兼容和样例 | 尚有价值的能力或内容不可恢复 | 先整理工作区、形成可构建提交和 1.7.0 归档标签，再按清单删除 |
| 断代范围只删测试、不删产品分支 | 表面上简化，核心复杂度仍存在 | 从 Schema、类型、注册、适配、UI、导出、文档和测试全链路审计 |
| App 版本从 1.7.0 重置为 1.0.0 引发混淆 | 内部制品被误判为降级或新旧混用 | 明确 1.7.0 是未分发原型线；归档隔离旧制品，正式版只认 Project V8 |
| 仓库拆分后工程引用外部路径 | 移动目录或离线打开即失效 | 外部目录只负责发现；导入后锁定哈希并嵌入工程与发布物 |
| 组件库过早积累课例专属组件 | 复用幻觉、维护成本和选择噪音 | 四级质量状态；至少两个独立课例验证后才晋升稳定库 |
| 中文化发生语义漂移 | Skill 触发、门禁或拒绝行为退化 | 保留英文稳定 ID与双语 description；冻结前后对照测试，不通过不替换 |
| 控制器拖动与按钮点击冲突 | 误操作、收起后难以拖动或底层课件被触发 | 全表面候选手势、视觉位移阈值、超过阈值后抑制原点击、指针捕获、限位和多输入测试 |
| 把翻页笔当成可识别的独立设备 | WebHID 权限、浏览器兼容和单 HTML 离线体验恶化 | 使用标准 KeyboardEvent 命令层；不依赖厂商、驱动或设备枚举 |
| 翻页笔绕过互动检查点 | 学生未完成任务也能跳到下一场景 | 简单模式统一走导航守卫；互动模式只发作者命令且无隐式切幕后备 |
| 翻页笔抢占组件按键或长按连跳 | 滑块失效、误翻多页 | 焦点/默认阻止/模态优先、方向键独立开关、忽略 repeat、去重和真实硬件测试 |
| 公式只解决视觉、不保存语义 | 后续编辑、导出和校验失真 | 保存结构化公式源，统一渲染/静态后备并报告降级 |
| 把评审辅助当成自动设计系统 | Token 改动意外重写节点，启发式分数压倒教学判断 | Token/安全区只存机器可读作者元数据；信息释放/视觉密度只读并明确近似，不自动修改工程 |
| 为动态过程过早增加第二捕获协议 | 时间线、副作用、导出语义和测试面失控 | 继续使用 `waitUntil()` + `prepareCapture()` 的单确定帧；只在真实课例证明需求后研究多帧 |
| 把三种表面塞进现有 Scene | 可选字段爆炸、非法状态、Player 分支失控 | Project V8 保持纯 Slide；未来用原型验证后的 Project V9 判别联合 |
| Flow 模仿 OneNote | DOCX 映射差、阅读顺序不稳定 | 首版采用结构化流式块 |
| 过早建设通用 3D | 范围失控、核心模式质量下降 | 继续组件/运行时；真实复用证据后立项 |
| 混合模式过早上线 | 两套不稳定编辑器互相放大问题 | 各表面独立生产可用后再组合 |
| 办公格式反向限制 HTML | 成品被迫静态化或模板化 | HTML 优先；兼容导出显式降级 |
| PDF 语义不清 | 无限画布无法确定页面、混合页尺寸混乱 | 镜头帧与项目打印配置 |
| 质量门禁只有自动测试 | 工程通过但结果一般 | 截图、录屏、渲染复查和人工接受状态 |

## 12. 实施前仍需确认的设计问题

以下问题在对应里程碑开始前必须通过样例和技术调研确认：

### 12.1 正式版 1.0 冻结结果与剩余决策

1. **已冻结：** Project V8 在断代后只保留 Slide 真相，移除旧 Project/Runtime/Component 兼容，并新增当前正式字段；不通过版本号伪迁移。
2. **已冻结：** Project 只保存控制器作者 `x/y`；整面手势、点击阈值、限位、贴边与键盘步长是 Player 行为，授课 `dx/dy` 只在会话存在。
3. **已冻结：** `playback.presenter` 保存启用、`scene-navigation/authored-command` 策略与精确附加按键；默认 PageUp/PageDown，作者命令没有隐式翻页后备。
4. **已冻结：** 按键检测、恢复/删除附加映射由属性栏完成，附加映射进入 Project 和 PublishedLesson，不是本机偏好。
5. **已冻结：** `FormulaNode` 使用七类递归 AST、稳定 Formula ID 和无障碍文本；共享 Canvas 渲染，PPTX 转独立透明图片并报告差异。
6. **已冻结：** 本地组件目录使用 `catalog.json`、SHA-256、精确版本锁定和显式更新；只有与发行版审核摘要完全匹配的内置 catalog 可免确认，外部组件多选导入以一个批次确认加入，更新、替换以及同 ID/版本不同哈希仍显式审阅或阻断。已嵌入的精确工程包可直接实例化，不重复读取目录或弹窗。
7. **阶段性冻结：** S6 只物理落地当前软件依赖的相邻 `courseware-components`，并冻结 `courseware-cases` 的职责和迁移清单；用户批准 W1 后才建立课例根目录并原子迁移历史旗舰/失败课例。是否分别建立远程 Git 仓库不阻塞软件基线，但不得把领域组件和新课例成品重新硬编码进核心。
8. **已冻结：** 当前主干只保留 Project V8/Runtime 2/Component 4 夹具；旧实现、样例与专属测试归档或删除。
9. **已冻结：** Project Health、Export Preflight JSON 和不含课件素材的异常诊断报告三者职责分开；设计 Token、安全区和只读启发式不收集额外用户内容。
10. **仍待工作流阶段决定：** 两个冷启动正式验收课例的学科、目标用户、课时、互动机制和人工审阅人。

### 12.2 多表面里程碑开始前

1. Project V9 中 Slide 是“一表面一个现有场景”，还是“一表面内部保留多个场景”。
2. Flow 首版块类型、嵌套深度、表格能力和公式表示。
3. Flow 的默认阅读宽度、响应式行为、A4/Letter 打印配置和页眉页脚。
4. DOCX 生成、字体、公式、表格和图片处理能力在 Flow 原型中的实际边界。
5. 混合 PDF 是统一页面尺寸，还是允许一个 PDF 中存在不同页面尺寸。
6. 通用课程控制器如何在固定画布、滚动文档和空间画布间自适应。
7. 跨表面状态、链接、目录和重播动作的统一命名。
8. Spatial 2D 的世界边界、镜头书签、语义缩放和 PDF 导出帧模型。
9. Flow/Spatial 组件的尺寸、响应式更新、捕获和文字编辑协议。
10. PublishedLesson V2 如何保留多表面运行数据而不主动交付作者态结构。

## 13. 下一批具体工作

当前不再重复已经完成的断代和核心实现。AI-native 基建 P0–P4 已按 [2026-08-12 验证记录](docs/reviews/AI_NATIVE_EDITOR_FOUNDATION_VERIFICATION_20260812.md) 完成类型检查、123 文件/777 项 Vitest、生产构建、组件 catalog 校验、AI 能力清单 `--check` 与始终隐藏的 Electron E2E 27/27；P5 仍只获准保留 RFC/测试原型。随后按以下顺序收口：

1. 首批九个 `experimental` 组件的最终隐藏 V8 矩阵已通过 2/2；catalog 已移除矩阵验证阻断，但继续保留许可、素材来源和维护人发布阻断，不提前晋升质量等级。
2. 同一工作树的类型检查、115 文件/717 项单元与集成测试、始终隐藏窗口的 Electron E2E、Player/Renderer/Electron 生产构建均已通过；单 HTML、网页包、PDF、PPTX 已真实生成并复查。
3. 历史 S6 的隐藏 Electron 26/26、组件矩阵 2/2、非 Electron 基线、生产构建、产物哈希、视觉结果和环境指纹已形成可追溯证据；当前增量基线更新为 123 文件/777 项 Vitest 与隐藏 E2E 27/27，软件本体仍只签发为 `engineering candidate`。
4. README、用户指南、创作/运行时/组件规范、计划和示例已经按当前能力同步；后台自动化保持 BrowserWindow 始终隐藏，只有显式可视调试脚本可以显示。
5. 软件本体达到 `engineering candidate` 并获用户批准后启动 W1：第一项建立 `courseware-cases`，按原状态原子迁移旗舰/失败课例及其脚本和证据；随后才启动 Project V8 实现 Skill 与通用/学科工作流，不得使用归档的 V7 Skill 生成当前工程。
6. W2 使用干净上下文生产两个不同学科、不同互动机制的全新冷启动课例；历史旗舰和失败课例均不计数。
7. W3 只在两个新课例完成 HTML、网页包、PDF、PPTX、截图、录屏和人工审阅并达到 `accepted` 后，才完成内部正式版 1.0 产品验收。
8. 内部正式版 1.0 验收后，再选择真实长文内容启动 Flow 原型；之后依次评估 Project V9、混合工程和 Spatial 2D。

## 14. 完成定义

### 14.1 内部正式版 1.0

以下条件同时满足时，才可把当前产品称为内部正式版 1.0：

- 应用版本、Project V8、Runtime API 2、Component API 4 和发布格式边界一致；
- 旧 Project、运行时和组件不会被静默加载，主程序不再背负旧兼容实现；
- 编辑器核心、组件库和课例库完成职责分离；
- 本地组件可以发现、审阅、导入、锁定、嵌入和显式更新，工程移动或离线时不失效；
- 成品 HTML 中教师控制器可安全拖动、收起和复位；
- PageDown/PageUp 标准翻页笔在整课预览、单 HTML 和网页包中即插即用，简单切幕与作者规则都不绕过检查点；
- 纵向分式、上下标和根式达到正式教学排版要求，所有导出降级可见；
- 四格式 Export Preflight 可定位并保存 JSON，确定性错误阻断、启发式警告不越权；`designTokens`、安全区、信息释放和视觉密度均保持机器可读/只读边界，不自动修改节点；
- 自动测试与制品验证默认保持编辑器和预览 BrowserWindow 始终隐藏，不干扰用户桌面；
- Skill 中文化通过冻结前向测试，并能从落盘制品恢复而不依赖聊天上下文；
- 两个不同学科、不同互动机制的冷启动课例达到人工 `accepted`；
- 单 HTML、网页包、PDF 和 PPTX 都经过真实文件与离线复查；
- 管线状态与结果状态完全分离，未获人工接受的成品不得称为正式质量通过；
- 当前 README、用户指南、开发规范和示例只描述真实能力；
- 许可证调整、代码签名、公开安装器、自动更新和外部分发不作为内部 1.0 的完成条件。

### 14.2 多表面路线

多表面路线只有在以下条件同时满足时才能称为完成：

- 用户能按内容意图选择正确表面，而不是按导出格式被动选择；
- Slide、Flow 和 Spatial 2D 都能独立完成创作、保存、预览、HTML 发布和模式专属兼容导出；
- 混合工程能统一导航、主题、素材、课程状态和 HTML 播放；
- Project V8 能显式迁移到实际落地的多表面工程版本；更早原型格式不重新进入正式版兼容范围；
- 所有兼容导出都有明确且经过真实文件复查的静态化语义；
- 管线状态与结果状态完全分离；
- 至少各有一个真实课例达到 `accepted`，而不是只有演示夹具或结构测试；
- 通用 3D 是否进入产品已基于真实需求作出明确决策。

最终产品定位不是“同时模仿 PPT、Word、OneNote 和 3D 软件”，而是：

> 以互动 HTML 为业务真相，根据教学内容选择幻灯片、流式文档或空间画布作为创作表面，并把 PPTX、DOCX 和 PDF 作为透明、可解释的兼容交付路径。
