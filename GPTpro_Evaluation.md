# UI/UX 四模式与“基础状态共同表面”设计定稿

> **历史材料提示（2026-08-17）：** 本文关于“取消教师可见全局层”“物化共享项并升级 V10”的结论已被用户后续明确撤回。当前权威结论是：Course Project V9 保持不变，`globalLayerItems` / `surfaceLayerItems` 继续存在，四态均保留可见“共享内容 → 全局层（全课）”作者入口。执行请以 [`COURSEWARE_DEVELOPMENT_PLAN.md`](COURSEWARE_DEVELOPMENT_PLAN.md) 与 [`V9_EDITOR_UI_DESIGN_SPEC.md`](V9_EDITOR_UI_DESIGN_SPEC.md) 为准；本文仅作历史推理记录。

> 日期：2026-08-16  
> 分支：`codex/v9-editor-v8-base`（HEAD `e2e34aa`）  
> 性质：产品语义定稿、UI/UX 方案与开发实施报告；未修改产品代码  
> 依据：现有仓库代码、此前深度审计、Opus《UI/UX 现状评估与四模式设计》及本轮用户定稿  
> 替代范围：本报告替代 Opus 报告第 3 章“幻灯片三层模型与锁定机制”和第 4 章“全局层改动的应用机制”，并修订其四模式界面草图

---

## 0. 结论摘要

本轮设计作出四项最终收敛：

1. **取消教师可见的“全局层”页面。**
2. **取消 Slide 中独立的“当前内容共用”编辑页面。**
3. **每一叠幻灯片直接以“基础画面”作为共同表面；命名状态只保存相对基础画面的差异。**
4. **锁定仅表示该节点不可被修改；锁定后仍可选择和查看，但唯一允许的写操作是“解锁”。**

本文将“一叠幻灯片”严格定义为：

> 一个 `SlideSceneDocument`，包含一个基础画面和若干命名状态。

它不是整个 `SlideSurfaceDocument` 下的所有场景，也不是传统 PPT 中跨全部页面的母版。

新的教师心智模型只有两层：

```text
一叠幻灯片 / 一个场景
├── 基础画面：该叠所有状态的共同内容与共同属性
└── 命名状态：只记录与基础画面不同的内容
```

不再存在：

```text
课程全局层页面
当前内容共用层页面
全局层待应用改动
锁定后冻结继承
全局层删除后的保留关系
跨页共享节点的隐式继承
```

### 0.1 新旧方案对照

| 事项 | 旧方案 | 本次定稿 |
|---|---|---|
| Slide 共同内容 | 全局层、Surface 共用层、场景基础三套来源 | 仅场景基础画面 |
| 命名状态 | 覆盖场景基础 | 保留，作为唯一继承机制 |
| 跨场景共享 | 持久共享节点与显隐名单 | 不建立持久继承；需要时使用模板、复制或一次性批量应用 |
| 全局层页面 | 左栏独立入口 | 删除 |
| 当前内容共用页面 | 左栏独立入口 | Slide 中删除 |
| 锁定 | 可能参与状态覆盖和继承语义 | 纯作者权限，节点级、跨该叠全部状态 |
| 锁定后可做什么 | 行为分散，部分入口仍能改 | 可选中、可查看、仅可解锁 |
| 全局改动 | 先编辑、后应用 | 整套机制取消 |
| 页面显隐 | `LocationVisibility` 与状态覆盖并存 | Slide 作者侧只使用基础可见性与状态覆盖 |
| 教师控制器 | 默认放在工程全局层 | 作为普通节点放入每个场景的基础画面 |

### 0.2 项目决策

不需要重写 V9，也不需要建立新的母版系统。

需要实施的是一次明确的模型收敛：

- Slide 作者界面只保留 `scene` 编辑范围；
- `scene.layerItems` 成为该叠幻灯片唯一节点清单；
- `presentation.states[].layerItemOverrides` 成为唯一状态差异；
- 现有 Slide 全局项和 Surface 共用项迁移为各场景基础节点；
- 所有节点写操作统一经过锁定守卫；
- 四模式信息架构继续采用，但所有模式都不再显示“全局层页面”。

---

## 1. 现状与本次方案的代码基础

### 1.1 当前代码已经有合适的基础—状态模型

现有 Slide 数据已经分成：

```ts
SlideSceneDocument {
  layerItems: LayerItem[]
  presentation?: {
    states: SlidePresentationState[]
  }
}
```

其中：

- `scene.layerItems` 是基础节点清单；
- 当前 UI 用 `activeStateId === null` 表示编辑基础画面；
- 命名状态通过 `layerItemOverrides`、`layerItemOrder`、背景覆盖保存差异；
- `slideEditorView.ts` 只对 `scene.layerItems` 应用命名状态覆盖。

`SceneStateStrip.tsx` 也已经把基础画面描述为：

> 所有命名状态的继承源。

因此，“基础画面担任共同表面”不是新增一套架构，而是把现有最清晰的一条主线提升为唯一主线。

### 1.2 当前多出的两类共享作者页面

`ScenePanel.tsx` 当前还提供：

- `global-layer-entry`：工程全局层；
- `surface-layer-entry`：当前内容共用层；
- `scene`：当前场景。

这使 Slide 作者同时面对三套节点归属，并需要理解：

- 工程级共享；
- Surface 级共享；
- Scene 级基础；
- 命名状态覆盖；
- `LocationVisibility`；
- 锁定；
- 显隐。

这些概念在当前阶段产生的理解成本，已经高于它们带来的编辑收益。

### 1.3 当前默认工程依赖全局层

`courseStudioModel.ts` 的新工程创建逻辑，当前把默认教师控制器写入：

```ts
project.globalLayerItems
```

因此删除全局层页面不能只改 UI；新工程初始化、新建场景、复制场景和旧工程迁移都必须同步调整。

### 1.4 当前锁定还不是统一权限

现有代码已经在部分手势路径中阻止锁定节点拖动，但删除、属性修改、部分双击编辑、状态覆盖、AI patch 等入口尚未统一使用同一个命令守卫。

本次定稿不再给锁定附加任何继承语义，反而使实现更简单：

```text
locked = true
→ 所有内容写命令拒绝
→ unlock 命令例外
```

---

## 2. 最终领域模型

### 2.1 课程、Surface、场景和状态

```text
Course Project
├── Slide Surface A
│   ├── Scene A1（一叠幻灯片）
│   │   ├── Base 基础画面
│   │   ├── State 提问
│   │   └── State 答案
│   ├── Scene A2（一叠幻灯片）
│   │   ├── Base 基础画面
│   │   └── State 解析
│   └── Scene A3
├── Flow Surface B
└── Spatial Surface C
```

### 2.2 “基础画面”不是普通命名状态

需要在产品文案和代码注释中明确区分：

| 概念 | 数据位置 | 含义 |
|---|---|---|
| 基础画面 | `scene.layerItems`、场景背景 | 节点清单和所有状态的继承源 |
| 命名状态 | `presentation.states[]` | 稀疏差异 |
| 运行初始状态 | `presentation.initialStateId` | 播放开始时进入的命名状态 |
| 缩略图状态 | `presentation.thumbnailStateId` | 左侧缩略图使用的命名状态 |

当前“基础”和名为“初始”的命名状态容易让教师误以为是同一件事。

建议 UI 文案固定为：

- **基础画面**：共同内容；
- **开始状态**：播放开始时显示；
- **缩略图状态**：左侧预览使用。

简洁模式中可隐藏技术区分，但内部语义不得合并。

### 2.3 节点唯一归属

Slide 中每个可编辑节点只归属于一个场景的基础节点清单：

```ts
scene.layerItems
```

命名状态不拥有第二份完整节点，只保存差异。

因此：

- 不存在“全局层节点”；
- 不存在“Surface 共用节点”；
- 不存在“锁定后脱离共享关系”；
- 不存在“全局层删除后某页保留旧副本”；
- 不需要“先编辑、后应用”的暂存机制。

### 2.4 基础画面编辑规则

在基础画面中：

| 操作 | 结果 |
|---|---|
| 新增节点 | 加入 `scene.layerItems`，默认被所有状态继承 |
| 移动、缩放、旋转 | 修改基础几何，未覆盖该字段的状态自动继承 |
| 修改文字、图片、样式 | 修改基础内容，未覆盖该字段的状态自动继承 |
| 隐藏节点 | 修改基础 `visible` |
| 调整顺序 | 修改基础 `order` |
| 删除节点 | 结构性删除；同时清除该节点的状态覆盖和交互引用 |
| 锁定节点 | 整个场景组内不可编辑 |
| 解锁节点 | 恢复编辑能力 |

基础删除影响该叠全部状态，必须给出明确确认：

```text
删除“校徽”？

它属于基础画面，删除后将从“提问”“答案”等所有状态中移除，
相关状态覆盖和互动引用也会一并清理。

取消    删除
```

### 2.5 命名状态编辑规则

在命名状态中：

| 操作 | 结果 |
|---|---|
| 修改基础节点 | 写入当前状态的稀疏覆盖 |
| 隐藏基础节点 | 写入 `visible: false` |
| 恢复某项 | 删除该字段覆盖，重新继承基础值 |
| 恢复整个节点 | 删除该节点全部覆盖 |
| 调整顺序 | 写入当前状态 `layerItemOrder` |
| 修改背景 | 写入状态背景覆盖 |
| 删除基础节点 | 在当前状态中隐藏，不做结构删除 |
| 清除状态 | 删除该状态全部覆盖 |

### 2.6 在命名状态中新建节点

为了保留“某个步骤才出现的新内容”，推荐沿用稀疏模型：

1. 节点仍写入 `scene.layerItems`；
2. 基础可见性设为 `false`；
3. 当前状态写入 `visible: true`；
4. UI 将其标记为“仅当前状态”。

```text
图层列表

👁 题目文字            来自基础
👁 解题箭头            仅“解析”状态
🔒 答案卡片            当前状态有覆盖
```

若节点以后需要出现在更多状态，可在状态管理中选择：

```text
显示范围
● 仅当前状态
○ 选择状态……
○ 设为基础内容
```

“设为基础内容”应：

- 将基础 `visible` 改为 `true`；
- 删除当前状态中多余的可见性覆盖；
- 保留其他状态已有的显隐覆盖。

### 2.7 在命名状态中删除节点

区分两种情况。

#### 基础节点

点击删除等价于：

```ts
currentState.layerItemOverrides[nodeId].visible = false
```

文案使用“在当前状态隐藏”，不要写“删除”。

#### 仅当前状态节点

若该节点：

- 基础不可见；
- 只在当前状态可见；
- 没有其他状态引用；
- 没有外部交互引用；

则允许直接结构删除。

否则仍只隐藏当前状态，并提示它还被其他状态使用。

---

## 3. 取消全局层页面后的 UI/UX

### 3.1 纯幻灯片模式

删除左栏中的：

- “全局层”入口；
- “当前内容共用”入口；
- 与二者相关的元素数量、动态内容徽标和不可编辑提示。

新版结构：

```text
┌─ 数学第三章 ─────────────────────────────────────────────────────┐
│  ⤺ ⤻    ▶ 试运行    ⬇ 导出                      简洁 │ 专业     │
├──────────────┬─────────────────────────────────┬─────────────────┤
│ 幻灯片    ＋ │                                 │ 元素 图层 属性  │
│              │     ┌─────────────────────┐     │                 │
│ ┌──────────┐ │     │                     │     │  文字           │
│ │▣ 1  封面 │ │     │                     │     │  图片           │
│ └──────────┘ │     │       幻灯片         │     │  形状           │
│ ┌──────────┐ │     │                     │     │  公式           │
│ │▣ 2  引入 │ │     │                     │     │                 │
│ └──────────┘ │     └─────────────────────┘     │                 │
│ ┏━━━━━━━━━━┓ │                                 │                 │
│ ┃▣ 3  例题 ┃ │  ┌── 画面状态 ───────────────┐  │                 │
│ ┗━━━━━━━━━━┛ │  │ ●基础  ○提问  ○答案  ＋  │  │                 │
│              │  └───────────────────────────┘  │                 │
│              │                                 │                 │
│ ⊕ 加入其他   │                                 │                 │
├──────────────┴─────────────────────────────────┴─────────────────┤
│ 幻灯片 3 / 8 · 基础画面 · 已选：文本 · 已保存                    │
└──────────────────────────────────────────────────────────────────┘
```

左栏现在只回答一个问题：

> 现在编辑哪一叠幻灯片？

画布下方状态条回答另一个问题：

> 现在编辑这一叠中的哪个画面状态？

### 3.2 状态条

```text
┌─ 画面状态 ──────────────────────────────────────────────────────┐
│ [基础画面] [提问 · 3 项覆盖] [解析 · 5 项覆盖] [答案 · 2 项覆盖] ＋ │
│ 共同内容    继承基础            继承基础            继承基础         │
└─────────────────────────────────────────────────────────────────┘
```

选中基础画面时：

```text
正在编辑共同内容。
此处修改会同步到本叠中所有未覆盖该属性的状态。
```

选中命名状态时：

```text
正在编辑“解析”的差异。
蓝点表示本状态覆盖；未覆盖的内容继续继承基础画面。
```

### 3.3 图层列表

基础画面：

```text
图层
👁 标题
👁 题目
🔒 校徽
👁 教师控制器
```

命名状态：

```text
图层
👁 标题                  继承基础
👁 题目                  继承基础
👁 解题箭头              仅当前状态
🔒 校徽                  已锁定
👁 答案卡片              当前状态有覆盖
```

不再显示：

- 全局；
- 当前内容共用；
- Scene；
- Global Runtime；
- Surface Runtime。

这些开发概念不应出现在普通教师的节点归属中。

### 3.4 混合模式

四模式信息架构继续保留，混合课程仍按教学顺序排列：

```text
┌─ 生物第五章 ─────────────────────────────────────────────────────┐
│  ⤺ ⤻    ▶ 试运行    ⬇ 导出                      简洁 │ 专业     │
├──────────────┬─────────────────────────────────┬─────────────────┤
│ 课程流程  ＋ │                                 │ 元素 图层 属性  │
│              │   ┌─────────────────────────┐   │                 │
│ ▾ ▣ 开场     │   │                         │   │                 │
│    1 封面    │   │        幻灯片            │   │                 │
│    2 提问    │   │                         │   │                 │
│              │   └─────────────────────────┘   │                 │
│ ▾ ▤ 知识讲解 │                                 │                 │
│    定义      │  ┌── 画面状态 ───────────────┐  │                 │
│    条件      │  │ ●基础  ○提问  ＋          │  │                 │
│              │  └───────────────────────────┘  │                 │
│ ▾ ⊞ 结构观察 │                                 │                 │
│    全景      │                                 │                 │
│    细胞      │                                 │                 │
├──────────────┴─────────────────────────────────┴─────────────────┤
│ 开场 · 幻灯片 2 / 2 · 基础画面 · 已保存                          │
└──────────────────────────────────────────────────────────────────┘
```

切到 Flow 或 Spatial 后：

- 左栏课程流程位置和宽度不变；
- 中间切换为讲义或空间工作区；
- 右侧切换为对应 Inspector；
- 不出现任何全局层入口。

### 3.5 纯讲义和纯空间模式

保持 Opus 报告的减法方向：

- 纯讲义左栏只有大纲；
- 纯空间左栏只有视角；
- 不挂跨类型导航；
- 不挂全局层页面；
- 控制器如需显示，作为当前内容中的普通节点或明确的播放控制设置，不建立用户可编辑的共享层页面。

---

## 4. 锁定机制定稿

### 4.1 定义

锁定只回答一个问题：

> 这个节点现在是否允许被修改？

它不表示：

- 是否显示；
- 是否继承；
- 是否脱离基础画面；
- 是否只属于某个状态；
- 是否跟随其他场景；
- 是否参与播放；
- 是否被全局层保护。

### 4.2 锁定范围

锁定属于基础节点本身：

```ts
LayerItemBase.locked
```

它不是命名状态覆盖。

因此一个节点一旦锁定：

- 该叠的基础画面中锁定；
- 所有命名状态中也锁定；
- 状态现有外观差异仍然照常显示；
- 任何状态都不能修改它，除非先解锁。

### 4.3 锁定后允许与禁止的操作

| 操作 | 锁定后 |
|---|---|
| 画布点选 | 允许 |
| 图层列表选中 | 允许 |
| 查看只读属性 | 允许 |
| 查看状态覆盖来源 | 允许 |
| 定位、缩放到节点 | 允许 |
| 解锁 | 允许 |
| 拖动 | 禁止 |
| 缩放、旋转 | 禁止 |
| 方向键微调 | 禁止 |
| 文字、公式就地编辑 | 禁止 |
| 属性栏写入 | 禁止 |
| 隐藏、显示 | 禁止 |
| 调整层级 | 禁止 |
| 删除 | 禁止 |
| 复制、剪切、创建副本 | 禁止 |
| 替换素材 | 禁止 |
| 编辑动画和互动 | 禁止 |
| 修改 Component Props | 禁止 |
| 修改 Runtime 作者字段 | 禁止 |
| AI patch | 禁止 |
| AI 自动解锁 | 禁止 |

“只可解锁”指唯一允许改变工程数据的动作是解锁；选择和查看属于只读行为。

### 4.4 锁定后的画布表现

锁定节点仍有可见选择框，但不显示可拖动手柄：

```text
┌─────────────────────────────┐
│ 🔒 校徽                      │
│                             │
│      锁定节点的内容          │
│                             │
└─────────────────────────────┘
```

建议：

- 选择框使用较弱的虚线；
- 左上角显示锁图标；
- 鼠标悬停提示“已锁定，点击属性栏解锁”；
- 双击不进入编辑；
- 不使用“点不中”表达锁定，否则用户无法查看和解锁。

### 4.5 锁定后的属性栏

```text
┌─ 属性 ─────────────────────────┐
│ 校徽                    🔒 已锁定│
│                                │
│ 该节点在本叠全部画面状态中只读。 │
│                                │
│ ┌────────────────────────────┐ │
│ │          解锁节点          │ │
│ └────────────────────────────┘ │
│                                │
│ X      120       Y       40     │
│ 宽      80       高       80     │
│ 旋转     0°      不透明度 100%   │
│                                │
│ 内容、样式、互动与动画均只读。   │
└────────────────────────────────┘
```

属性栏不能只把输入框设为 disabled，还要在命令层拒绝写入。

### 4.6 锁定命令

建议建立一个唯一守卫：

```ts
assertLayerItemEditable(item, operation)
```

所有写命令必须调用，包括：

- transform；
- property update；
- text/formula edit；
- visibility；
- reorder；
- delete；
- duplicate；
- asset replace；
- component props；
- runtime content；
- animation/interaction；
- state override；
- AI authoring patch。

唯一例外：

```ts
unlockLayerItem(...)
```

锁定本身也应使用明确命令：

```ts
lockLayerItem(...)
```

AI 不得调用解锁命令，除非用户的当前指令明确包含“先解锁”。

### 4.7 移除状态级锁定

当前 `LayerItemOverride` 允许 `locked?: boolean`。这与本次语义冲突。

目标模型：

```ts
interface LayerItemOverride {
  // 不再包含 locked
}
```

迁移规则采取保守策略：

1. 基础节点本身已锁定，保持锁定；
2. 任一命名状态有 `locked: true`，基础节点设为锁定；
3. 删除全部状态中的 `locked` 覆盖；
4. 迁移报告列出由状态锁升级为节点锁的数量。

这样不会因迁移意外把原本受保护的节点解锁。

---

## 5. 跨场景重复内容如何处理

取消全局层后，校徽、页码、教师控制器等重复内容不再建立永久共享关系。

采用三种显式工具。

### 5.1 创建模板

新建幻灯片时可选择：

```text
新建幻灯片
● 空白
○ 复制当前幻灯片
○ 从模板创建
```

模板只在创建时复制，不持续继承。

### 5.2 一次性复制到其他幻灯片

节点菜单：

```text
复制到……
- 下一张幻灯片
- 选择幻灯片……
- 本组全部幻灯片
```

复制完成后，每个场景拥有独立节点。

### 5.3 一次性批量应用

对于需要统一调整的简单属性，可提供显式批处理：

```text
将当前节点的样式应用到……
- 选中的 6 张幻灯片
- 当前 Slide Surface 的全部幻灯片
```

它是一次普通事务：

- 立即写入各场景；
- 一次 Undo 可整体撤销；
- 不建立后续继承；
- 不产生“待应用”状态；
- 不受全局层概念影响；
- 遇到锁定节点时跳过并列出结果。

结果示例：

```text
已更新 12 张幻灯片
跳过 3 个锁定节点
2 张幻灯片未找到同名节点
```

### 5.4 节点匹配

批量应用不得仅依赖显示名称。

推荐优先级：

1. 用户明确选中的目标；
2. `templateRole` 或稳定语义标签；
3. 同源复制产生的 `originId`；
4. 最后才是名称匹配。

`originId` 仅表示复制来源，不产生继承关系。

---

## 6. 数据模型调整

### 6.1 Slide 的目标不变量

V10 Slide 工程应满足：

```text
1. 教师可编辑视觉节点全部存在于 scene.layerItems。
2. 命名状态只存稀疏覆盖。
3. Slide Surface 的 surfaceLayerItems 不再用于教师作者节点。
4. project.globalLayerItems 不再用于 Slide 教师视觉节点。
5. locked 只存在于基础 LayerItem。
6. Slide 编辑器没有 global/surface authoring scope。
```

### 6.2 不应误删的课程级能力

取消“全局层页面”不等于删除所有课程级概念。

以下仍可保留：

- `globalInteractions` 中不依赖视觉节点的课程级规则；
- `courseState`；
- `navigationGuards`；
- 全课程播放设置；
- 设计令牌；
- 资源库；
- 混合课程流程；
- 导出与打印计划。

需要删除的是教师可见的共享视觉作者页面，不是课程运行逻辑。

### 6.3 `globalLayerItems` 和 `surfaceLayerItems`

建议分两阶段处理。

#### 第一阶段：弃用而不立即删除字段

- UI 不再展示；
- 新工程不再写入 Slide 视觉节点；
- Slide 新增命令不再接受 global/surface scope；
- 打开旧工程时执行迁移；
- 健康检查将遗留 Slide shared items 标为迁移问题；
- 兼容导入器仍可读取 V9。

#### 第二阶段：Schema 清理

在确认：

- V9 迁移稳定；
- Player 和导出不依赖旧共享层；
- Flow/Spatial 有自己的明确节点归属；
- 所有测试通过；

再决定是否从 Schema 中移除或限制字段。

### 6.4 Schema 版本

这不是纯 UI 改动，因为：

- 默认控制器归属改变；
- 状态级锁定语义改变；
- 旧共享节点需要物化；
- 老版本应用无法正确理解新不变量。

因此建议升级为：

```ts
COURSE_PROJECT_SCHEMA_VERSION = 10
```

不要在 `schemaVersion: 9` 下静默保存已经改变语义的工程。

---

## 7. V9 → V10 迁移

### 7.1 迁移目标

将旧工程中的 Slide 视觉共享项转换为各场景基础节点。

输入可能包括：

```text
project.globalLayerItems
slideSurface.surfaceLayerItems
LocationVisibility
globalInteractions
Runtime nodeBindings
状态级 locked 覆盖
```

输出：

```text
scene.layerItems
scene.interactions
基础 locked
无 Slide 作者共享项
```

### 7.2 迁移步骤

#### 第一步：建立位置映射

对每个 Slide Scene 找到其基础课程位置：

```text
surfaceId + sceneId → locationId
```

#### 第二步：计算有效共享项

对每个 Scene 分别计算：

- 在当前 location 可见的 `globalLayerItems`；
- 在当前 location 可见的 `surfaceLayerItems`；
- 当前 scene 原有 `layerItems`。

必须完整消费旧 `LocationVisibility` 的：

- `all`；
- `include`；
- `exclude`。

#### 第三步：物化节点

每个有效共享节点复制到目标 `scene.layerItems`。

建议生成确定性 ID：

```text
migrated::<sourceLayerItemId>::<sceneId>
```

若长度或字符集受限，则使用稳定哈希，但迁移映射必须保存到本次迁移上下文。

#### 第四步：合并层级

按照旧的全局有效顺序生成新的 `order`：

```text
global + surface + scene
→ 当前播放时的有效前后关系
→ 重新编号为 Scene 内稳定顺序
```

迁移前后截图应像素等价。

#### 第五步：迁移引用

需要重写：

- 触发器中的 nodeId；
- 动画动作中的 nodeId；
- 视频动作中的 nodeId；
- Runtime `nodeBindings`；
- Component/Runtime 作者地址；
- 教师控制器动作；
- AI authoring inventory 路径；
- 其他稳定引用。

课程级但不引用视觉节点的 `globalInteractions` 保持不动。

引用共享视觉节点的规则，需要按 Scene 复制到 `scene.interactions` 并使用该 Scene 的新 ID。

#### 第六步：迁移锁定

- 计算每个节点所有状态中的有效锁定值；
- 只要任一状态要求锁定，基础节点设为 `locked: true`；
- 清除状态 override 中的 `locked`；
- 输出迁移摘要。

#### 第七步：清理旧数据

仅在以下检查全部通过后，才删除旧 shared item：

- 所有引用可解析；
- 层级顺序合法；
- 组件包仍存在；
- Runtime 仍可运行；
- 状态覆盖未产生悬空 ID；
- 迁移前后静态预览一致。

### 7.3 特殊情况

#### 全局 Runtime

当前作者 Workspace 对多 Runtime、API 3 和 Surface Runtime 仍有限制。迁移工具不得静默复制后宣称成功。

处理规则：

- 可安全物化的 API 2 Runtime：按 Scene 复制并重写绑定；
- API 3、多个 Runtime 或无法确定作用域：阻止自动保存为 V10，生成明确报告；
- 用户可以继续用兼容只读模式打开，直到迁移能力补齐。

#### 教师控制器

默认教师控制器迁移到每个 Scene 基础画面。

新建 Scene 时：

- “空白场景”按产品默认决定是否加入控制器；
- “复制当前场景”复制控制器及其锁定状态；
- 控制器是普通节点，受同一锁定规则约束。

#### ID 冲突

若旧 Scene 已有与迁移 ID 冲突的节点：

- 不覆盖；
- 生成新的稳定 ID；
- 更新全部引用；
- 在迁移日志中记录。

### 7.4 迁移确认

迁移前显示一次摘要：

```text
升级此工程到 V10？

将进行以下调整：
- 3 个全局节点复制到 8 张幻灯片
- 1 个当前内容共用节点复制到 4 张幻灯片
- 2 个状态级锁定升级为节点锁定
- 6 条互动引用将重新绑定

原文件不会被覆盖，首次保存时将另存为 V10 工程。

取消    升级并打开
```

---

## 8. 代码改造清单

| 文件 / 模块 | 主要改动 |
|---|---|
| `src/renderer/ui/ScenePanel.tsx` | 删除 `global-layer-entry`、`surface-layer-entry`；精简 `ScenePanelDocumentControl` |
| `src/renderer/ui/SceneStateStrip.tsx` | 强化“基础画面＝共同内容”；移除 global/surface scope 的空状态文案 |
| `src/renderer/App.tsx` | 删除 Slide 的 `onActivateGlobal`、Surface 激活和相关属性接线 |
| `src/renderer/course/v9SlideVerticalSlice.ts` | Slide 编辑范围固定为 scene；移除或封存 global/surface transform、delete、duplicate、reorder 路径 |
| `src/renderer/course/courseStudioModel.ts` | 默认教师控制器写入 Scene 基础；新建和复制 Scene 规则更新 |
| `src/shared/courseProjectTypes.ts` | V10 语义；从 `LayerItemOverride` 移除 `locked` |
| `src/shared/courseProjectSchema.ts` | V9→V10 迁移、严格不变量和版本升级 |
| `src/renderer/course/slideEditorView.ts` | 保留基础＋状态物化主线；不再拼接教师可编辑 global/surface Slide 节点 |
| `src/renderer/phaser/EditorScene.ts` | 锁定节点保持可选中；禁用拖动、缩放、旋转和双击编辑 |
| `src/renderer/ui/PropertiesTab.tsx` | 锁定时全部写控件只读，只保留解锁 |
| `src/renderer/ui/NodesTab.tsx` | 锁定行仍可选；隐藏或禁用所有写操作，仅保留解锁 |
| Runtime/Component 作者会话 | 开始编辑和提交时都检查锁定，防止会话期间被锁 |
| Interaction/Animation 编辑器 | 对锁定目标禁止新增、修改和删除相关规则 |
| AI patch / inventory | 暴露 `locked` 只读信息；统一走命令守卫；不得隐式解锁 |
| `src/shared/courseProjectModel.ts` | 有效层迁移、ID 重写、引用检查 |
| Player / export | 验证迁移后物化结果与原播放一致 |
| E2E / unit tests | 增加 V10 迁移、基础继承和锁定矩阵 |

### 8.1 需要删除或替换的术语

| 旧文案 | 新文案 |
|---|---|
| 全局层 | 删除 |
| 幻灯片全局层 | 删除 |
| 当前内容共用 | 删除 |
| 母版式全局层 | 删除 |
| 控制与全局 | 控制 / 播放控制 |
| 场景状态 | 画面状态（简洁模式） |
| 基础场景 | 基础画面 |
| 正在编辑覆盖值 | 正在编辑当前画面与基础的差异 |
| 全局元素 | 重复内容 / 模板内容，视具体场景而定 |

---

## 9. 开发顺序

### Gate A：模型与迁移测试

先写测试，不先改 UI：

1. V9 shared item 的有效可见性计算；
2. 物化到每个 Scene；
3. 层级顺序保持；
4. 状态覆盖引用保持；
5. nodeId 和 Runtime bindings 重写；
6. 状态级锁定升级；
7. 迁移失败不破坏源文件；
8. V10 严格 Schema。

### Gate B：锁定成为统一权限

1. 建立中央 `assertLayerItemEditable`；
2. 让全部人工命令接入；
3. 让 AI patch 接入；
4. 让 Runtime/Component 作者会话接入；
5. 锁定节点可选中；
6. 属性栏只读；
7. 只有 unlock 能写。

此 Gate 应先于大规模 UI 重构，因为后续所有界面都依赖锁定语义可信。

### Gate C：移除全局作者页面

1. 删除 ScenePanel 两个入口；
2. Slide 路由永远使用 scene scope；
3. 新工程不再创建 global controller；
4. 新建 Scene 在基础画面创建本地控制器；
5. 删除相关状态条文案、空态和快捷入口；
6. 更新视觉 baseline。

### Gate D：基础—状态编辑闭环

完整验证：

- 基础新增；
- 基础修改；
- 命名状态覆盖；
- 命名状态隐藏；
- 状态专属节点；
- 恢复单项；
- 清除全部覆盖；
- 基础结构删除；
- Undo/Redo；
- 保存重开。

### Gate E：四模式壳层

在模型正确后实施：

- 纯模式左栏做减法；
- 混合模式使用稳定课程流程；
- 切换类型时壳层不变；
- 左栏可折叠；
- 1366×768 画布面积占比达到至少 45%；
- 建立最小设计令牌。

### Gate F：跨场景效率工具

最后增加：

- 从模板新建；
- 复制到其他幻灯片；
- 一次性批量应用；
- 迁移来源 `originId`；
- 批处理结果摘要。

这些工具不能重新引入持久共享关系。

---

## 10. 验收标准

### 10.1 UI 验收

- [ ] 纯 Slide 左栏不再出现“全局层”。
- [ ] 纯 Slide 左栏不再出现“当前内容共用”。
- [ ] 左栏只显示幻灯片列表。
- [ ] 画布下方明确显示“基础画面”和命名状态。
- [ ] 基础画面文案说明其为共同内容。
- [ ] 命名状态可显示覆盖数量。
- [ ] 图层列表能区分“继承基础”“当前状态覆盖”“仅当前状态”。
- [ ] 锁定节点可被画布和图层列表选中。
- [ ] 锁定属性栏只显示只读值和一个主要“解锁”按钮。
- [ ] 混合模式切换 Slide/Flow/Spatial 时左栏结构不跳变。
- [ ] 1366×768 下内容主体占屏不低于 45%。

### 10.2 基础与状态功能验收

- [ ] 基础新增节点后，所有命名状态都能看到。
- [ ] 某状态修改节点后，基础和其他状态不改变。
- [ ] 删除状态覆盖后重新继承基础。
- [ ] 当前状态隐藏基础节点不会结构删除。
- [ ] 当前状态新建节点默认只在当前状态显示。
- [ ] “设为基础内容”后所有未显式隐藏的状态可见。
- [ ] 基础删除会清理全部状态覆盖和交互引用。
- [ ] 一次手势只产生一条历史记录。
- [ ] 保存重开后继承关系完全一致。

### 10.3 锁定功能验收

对 Native、Component、Runtime 作者目标逐项验证：

- [ ] 可选中。
- [ ] 不可拖动。
- [ ] 不可缩放。
- [ ] 不可旋转。
- [ ] 不可方向键微调。
- [ ] 不可双击编辑。
- [ ] 不可属性修改。
- [ ] 不可显隐。
- [ ] 不可删除。
- [ ] 不可复制。
- [ ] 不可层级调整。
- [ ] 不可替换素材。
- [ ] 不可修改互动和动画。
- [ ] AI patch 被命令层拒绝。
- [ ] 解锁后全部正常恢复。
- [ ] 任意状态中的锁定状态一致。
- [ ] 保存重开后锁定状态一致。

### 10.4 迁移验收

- [ ] 新 V10 工程 `globalLayerItems` 不含 Slide 作者视觉节点。
- [ ] Slide `surfaceLayerItems` 不含教师作者节点。
- [ ] V9 默认全局教师控制器被正确复制到各 Scene 基础。
- [ ] `include`/`exclude` 位置规则迁移正确。
- [ ] 迁移前后 Slide 静态截图一致。
- [ ] 迁移前后播放路径一致。
- [ ] 所有节点引用无悬空。
- [ ] 多场景 ID 不冲突。
- [ ] 锁定覆盖迁移不会意外解锁。
- [ ] API 3 / 多 Runtime 无法安全迁移时明确阻止，不静默降级。
- [ ] 原 V9 文件保持不变。
- [ ] V10 文件不能被旧构建误写回 V9。

### 10.5 生产 E2E

新增：

```bash
npm run test:e2e:v10-production
```

要求：

- 从默认新工程启动；
- 不使用 `v8-only`；
- 不使用只含一个文字节点的测试后端代替生产入口；
- 覆盖 1280×720、1366×768、1440×900、1920×1080；
- 覆盖 Windows 125% 和 150% 缩放；
- 保存 screenshot、trace 和 video；
- CI 产出可下载 Electron artifact；
- PR 必须通过后才能继续新增功能面。

---

## 11. 风险与边界

### 11.1 最大风险：旧共享 Runtime

普通 Native、Component 和教师控制器可以确定性复制；Runtime 涉及：

- API 版本；
- DOM 作者目标；
- nodeBindings；
- 多 Runtime；
- Scene/Global scope；
- Player carrier 限制。

必须将其列为迁移专项，不能为追求“一键升级”而丢失能力。

### 11.2 “基础画面”和“开始状态”混淆

当前新工程同时存在基础画面和名为“初始”的命名状态。

UI 必须通过文案、层级和颜色区分：

- 基础是共同内容；
- 开始状态是播放入口。

### 11.3 锁定旁路

只在按钮上 disabled 不足以构成锁定。

快捷键、AI、Runtime 消息、属性提交、异步素材选择、拖动提交都可能绕过 UI，必须在命令层做最后检查。

### 11.4 旧版兼容

V10 不能在旧版中无提示打开并覆盖。

建议：

- 旧版检测高版本后只读或拒绝；
- V10 首次迁移默认另存；
- 迁移日志随工程或诊断报告保留。

### 11.5 不在本专项中解决的事项

本报告不把以下内容伪装成已解决：

- Flow 正文直接编辑；
- API 3 Runtime 完整作者宿主；
- 同场景多 Runtime 作者能力；
- Surface Runtime；
- V9/V10 图片替换闭环；
- 公式画布直接编辑；
- 完整复制粘贴；
- 视觉令牌全量替换。

它们仍需各自 Gate，但不能阻止本次模型收敛。

---

## 12. 最终定稿清单

| # | 事项 | 状态 |
|---|---|---|
| 1 | 删除教师可见的全局层页面 | 已定 |
| 2 | 删除 Slide“当前内容共用”页面 | 已定 |
| 3 | 一个 Scene 定义为一叠幻灯片 | 已定 |
| 4 | 基础画面承担该叠的全部共同内容 | 已定 |
| 5 | 命名状态只保存相对基础的稀疏差异 | 已定 |
| 6 | 不建立跨 Scene 的持久视觉继承 | 已定 |
| 7 | 重复内容通过模板、复制或一次性批量应用处理 | 已定 |
| 8 | 全局层“先编辑、后应用”机制取消 | 已定 |
| 9 | 锁定是纯节点写权限 | 已定 |
| 10 | 锁定节点可选中、可查看 | 已定 |
| 11 | 锁定后唯一允许的写操作是解锁 | 已定 |
| 12 | 锁定跨该叠全部状态，不允许状态级锁定 | 已定 |
| 13 | 人工操作与 AI 使用同一锁定守卫 | 已定 |
| 14 | 默认教师控制器迁入每个 Scene 基础画面 | 已定 |
| 15 | `globalInteractions` 等课程级非视觉逻辑不因取消页面而删除 | 已定 |
| 16 | V9→V10 使用显式迁移，不在 V9 下静默改语义 | 建议作为实施门禁 |
| 17 | 旧共享 Runtime 无法安全迁移时阻止升级，不降级 | 建议作为实施门禁 |
| 18 | 四模式信息架构保留，但所有模式均无全局层入口 | 已定 |

---

## 13. 最终判断

这次调整比“改造全局层”更符合项目当前阶段。

它把 Slide 作者模型从：

```text
工程全局层
＋ Surface 共用层
＋ Scene 基础
＋ 命名状态覆盖
＋ 位置显隐
＋ 锁定继承
```

收敛为：

```text
Scene 基础画面
＋ 命名状态差异
＋ 节点级锁定
```

其主要收益是：

- 教师能直接理解；
- UI 左栏显著简化；
- 状态模型与现有代码主线一致；
- 不需要建立新的暂存应用范式；
- 不需要解释锁定与继承的复合关系；
- AI 定位和修改路径更单一；
- 保存、Undo/Redo、迁移和测试边界更清晰；
- 后续 Flow、Spatial 和 Runtime 作者能力可以在统一权限模型上继续发展。

实施原则应固定为：

> **先完成 V10 迁移与锁定 Gate，再删除旧页面；先保证数据与命令语义正确，再重做四模式视觉壳层。**
