# DeepSeek 交叉评估：对 GLM 与 SeedPro 两份研判报告的评审

- 日期：2026-08-13
- 作者：DeepSeek（AI 研判，非人类审批，不具备任何批准效力）
- 评估对象：
  - [GLM_evaluate.md](GLM_evaluate.md)（GLM 对 W2 问题说明的研判与 Skill 重构方案）
  - [seedpro_evaluate.md](seedpro_evaluate.md)（SeedPro 评估报告）
  - 对照：[deepseek_evaluate.md](deepseek_evaluate.md)（本文作者的独立研判）
- 范围：交叉评审与方案收敛建议。未修改任何代码、Skill 文件或其他评估报告（本说明本身除外）。
- 事实核查：对两份报告的关键技术断言做了源码抽查，结果见第五节。

## 一、总评

两份报告都是高质量的独立研判，都做到了问题说明要求的"不简化为补文案、不围绕 PDF/PPTX 反向主导"。三方（GLM / SeedPro / 本作者）根因分层高度收敛，P0 清单重合度约 80%。差异集中在执行顺序、门禁机械实现方式和控制器平面迁移的激进程度上，属于可裁决的分歧，不存在方向性对立。

## 二、三方共识（可直接作为裁决基础，无需再讨论）

1. 根因分层准确；更底层的问题在于：控制器合成模型、`visible` 声明制 vs 证明制、单机判题能力缺失；
2. 按**内容类别门禁**编辑性，不设硬比例；
3. 判定权威分层（三方命名不同但结构一致：有限选项自动 / 规范化短答案自动 / 数学等价需真引擎否则降级 / 开放语义表达永不硬锁）；
4. 容量预算按"必答单元 × 时间 + 重试 + 教师讨论"可机械求和，超预算阻止 `implementation-ready`；
5. `engineering candidate` 必须拆分行为子条件，Schema/导出绿色不再够；
6. 动作真实性：E2E 必须经真实控件，禁止直写 Presentation State 冒充；
7. 产品侧必先行：`ensureTeacherController` 自愈修复 + 控制器平面决策；
8. 否决清单一致：扩大正则、硬比例、PPTX 对象级重构、纯文案补丁。

## 三、各自的独特贡献（值得吸收）

### GLM 独有、且价值最高的一点

`pointerEvents: none` 观察——经源码抽查确认 `RuntimeHost.ts:155/162/176` 的 `:host` 与容器层确为 `pointer-events: none`，`PlayerApp.ts:82/219/398` 亦有相关设置。这意味着**"被全屏 Runtime 覆盖"可能只造成视觉遮挡而不阻断点击**。后果：控制器门禁必须**同时做截图测试（视觉）和交互测试（功能）**，二者不可互代。

其余可吸收：
- "自愈缺口 + 门禁缺口"双重失效的表述（`ensureTeacherController` 不修已有 controls，坏状态永续）；
- W2 这类高互动课例不应走 `fast` 路径；
- `STATE-*` 触发来源标签（`student-action | teacher-action | system-derivation`）作为动作真实性的机械判定手段；
- 明确的执行顺序建议。

### SeedPro 独有、且价值最高的两点

1. **教师导航权威语义**：控制器"上一幕/下一幕/重播"应永远可用，即使 scene 导航规则返回 blocked——教师操作不执行 scene 级 blocking 检查。源码抽查确认 `PlayerApp.nextScene()/previousScene()/replayScene()`（`PlayerApp.ts:465-486`）与 `navigation:blocked` 事件（`CourseRuntimeKernel.ts:253`）均存在，该方案技术上可行。这补上了"逃生路径"的机制层实现，比"加个继续按钮"更根本；
2. **机械门禁三件套**（廉价、可机械判定、直接命中 W2 失真）：
   - E2E 禁用 API 列表：禁 `setPresentationState()` / `events.emit()` 伪造完成 / "完成当前步骤"方法；
   - Inventory `visible` 实体 bounds 重叠度 <70% 几何检查；
   - "完成按钮陷阱"静态检测：某 STATE 只有"完成"按钮且无输入检查即报警。

其余可吸收：具体时间系数表（可直接作为默认档）、`src/shared/answerEvaluation.ts` 落点、存量课件 Player 加载时自动修复 controls、分阶段时间表与成功标准。

### 本作者报告独有（供交叉补全）

- 交互系统触发器无"答案匹配"类型——**T2（规范化短答案）在产品 API 中同样缺失**，不只是缺数学等价层；
- `acceptance_scope` 纳入新字段使旧验收自动失效；
- 编辑闭环按载体类别取样（native 文本 / 公式 / runtime text / 素材各至少一）；
- agent-browser 作为 E2E 执行器固化；
- 问题说明 9 条候选门禁收敛为 6 条最小充分硬门禁。

## 四、主要分歧点（需人类裁决）

| 分歧 | GLM | SeedPro | 本作者 | 建议 |
| --- | --- | --- | --- | --- |
| 控制器平面迁移时序 | 倾向独立 DOM，但先修自愈+同步再决定 | **必须**独立 DOM，直接给 z=1000 方案 | 两步走：近期一致性修复，中期 DOM 化 | 三方终局一致（DOM 平面），只差时序。可先修自愈 + 门禁，DOM 化另案验证 |
| 动作真实性优先级 | P1（用 STATE 标签替代 E2E 硬压） | P0（E2E 禁用 API 白名单 + 证据留存） | P0 | 门禁必须拦住 W2 型失真，建议 P0；实现可先用 SeedPro 机械三件套，E2E 作为证据层 |
| L3 数学等价自动判定后是否允许作导航门禁 | 有引擎则可自动 | 即使有引擎也只"给正向反馈"，**永不**作唯一门禁 | 有引擎则自动判定 | 采纳 SeedPro 更保守版本：等价匹配只给反馈不作硬门禁，更安全 |
| 容量超限阈值 | "≤ 时长 × 容差"（未给数） | 1.2× 警告 / 1.5× 阻止 | 总和 ≤ 时长即阻止 | 建议 SeedPro 数值 + "超预算必回编排删减/降级采集" |
| 编辑性档位合同优先级 | P1 | P0 | P0 | 建议 P0：Builder 无对照目标才敢全扔 Runtime |

## 五、事实核查结果（对双方关键断言的源码抽查）

| 断言 | 结论 |
| --- | --- |
| GLM：Runtime DOM 容器 `pointerEvents: none`，视觉遮挡 ≠ 功能阻断 | **属实**（`RuntimeHost.ts:155/162/176`、`PlayerApp.ts:82/219/398`）。本次评估最重要的新事实 |
| SeedPro：`nextScene/previousScene/replayScene` 存在、`navigation:blocked` 事件存在 | **属实**（`PlayerApp.ts:465-486`、`CourseRuntimeKernel.ts:253`） |
| SeedPro：renderTeacherController "900 行" | 基本属实（实际 855 行） |
| SeedPro 行号引用（PlayerApp L95-101 等） | 大致对得上，个别偏移，不影响结论 |
| 双方对 `createProject.ts` / `editorStore` / `controllerVisible` 的复述 | 与问题说明和独立探索一致，未见错述 |

未发现任何一方有实质性事实错误。SeedPro 的"手势代码/无障碍 DOM 镜像"是设计评价而非可证伪事实，且 855 行规模本身支持"渲染层过重"的判断。

## 六、各自弱点

### GLM

- P0-5 把"等价答案矩阵/编辑闭环"列为 candidate 子条件，但正文又将其标为 P1，存在轻微自相矛盾；
- 未指出 T2（规范化短答案）在产品 API 中同样缺失（只提了数学等价层），低估了判题能力缺口的面；
- 新校验脚本（`validate_teacher_controller_reachability.py`）的执行载体（谁跑真实 Player、谁截图）没交代，落地上比 SeedPro 的"禁用 API + 几何检查"更依赖额外设施。

### SeedPro

- 对"数据模型完全不动，只改渲染路径"过于乐观：编辑器里控制器是 Phaser 画布节点可拖拽编辑，若播放走 DOM、编辑走 Phaser，所见即所得会出现双轨不一致——其 7.1#2 自己提了，但方案正文未解决；
- "教师点下一幕永远能走"未考虑"揭示优先"的教学约束（部分课要求先揭示再推进），产品语义需补一条边界；
- 时间系数表数值是拍脑袋的（0.5/1/1.5 分钟等），可作默认值但须标注为"可调参数"而非定理。

### 本作者报告的两处不足（对号入座）

- 未做视觉遮挡与功能阻断的区分（GLM 已补）；
- 未提教师导航权威语义（SeedPro 已补）。

## 七、裁决建议

三方报告已构成一个可执行的收敛方案。以共识部分为骨架，吸收 GLM 的 pointerEvents 双重测试 + STATE 触发来源标签，吸收 SeedPro 的教师导航权威 + 机械三件套 + 时间系数 + 存量自动修复，将 6 条门禁升级为：

1. **控制器可达性 = 视觉（截图）+ 功能（真实点击）双证**；
2. **逃生路径 = 规则层面"教师导航不执行 blocking 检查" + 每幕错误/空白态证据**；
3. **动作真实性 = STATE 触发来源标签 + E2E 禁用 API 白名单 + 完成按钮陷阱检测**；
4. **判定矩阵 = L1/L2 五类矩阵、L3 只反馈不门禁、L4 永不硬锁**；
5. **编辑闭环 + `visible` bounds 重叠度 <70% 几何门禁**；
6. **工程一致性 + 容量预算（1.2× 警告 / 1.5× 阻止，超限回编排删减）**。

收敛后剩下的唯一真问题是**产品侧决策**，需人类在任何 Skill 重构前拍板：

- 控制器 DOM 化时序（先修自愈+门禁过渡，还是直接迁独立平面）；
- L3 数学等价引擎是否纳入编辑器产品路线；
- Runtime Authoring 的 bounds 上报能力是否纳入路线。

## 附：三份报告定位对照

| 维度 | GLM | SeedPro | DeepSeek |
| --- | --- | --- | --- |
| 根因分层 | 准确，补自愈缺口 | 准确，补产品优先级固化 | 准确，补交互系统无答案触发器 |
| 控制器平面 | 倾向 DOM，两步走 | 必须 DOM，激进方案 | 两步走，DOM 为中期 |
| 编辑性边界 | 类别门禁（三档 editabilityTier） | 类别归属表 + bounds 重叠检查 | 类别门禁 + 定位证据 |
| 判题模型 | 五档 judgmentAuthority | 四级 L1–L4 + 教师权威 | 四级 T1–T4 |
| 容量预算 | 必答单元可求和 | 具体时间系数 + 1.2/1.5 阈值 | 默认时间档 + 阻止实现 |
| 动作真实性 | STATE 触发来源标签 | 禁用 API 白名单 + 证据留存 | 清单比对 + 禁直写状态 |
| 独特事实贡献 | pointerEvents 双重测试 | 教师导航权威、机械三件套 | 无答案触发器、scope 失效、执行器固化 |
| 总体判断 | 方案收敛、执行顺序清晰 | 落地最具体、可操作性强 | 门禁收敛、分层最薄 |
